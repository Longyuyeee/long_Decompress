# Long解压开发审计与后续计划

> 审计日期：2026-07-30
> 审计基线：`master` / `e1e0b6b`
> 当前正式版本：v1.0.19

## 1. 审计结论

当前版本已经具备稳定的压缩、解压、密码处理、任务取消、事务回滚、托盘、传统右键菜单和应用内更新闭环。
本轮没有发现阻断 v1.0.19 继续使用的 P0 故障。发布链、主线保护和自动化门禁均处于健康状态。

架构演进已经完成四个安全边界：

1. 归档格式识别、能力矩阵和执行路由；
2. 暂存目录、资源限制、冲突决策、事务提交和失败回滚；
3. TAR 系列与 GZ/BZ2/XZ/Zstandard 单文件流原生解压；
4. ZIP 原生解压、全条目密码预检和异常路径缓冲区归还。

`compression_service.rs` 已从 4,281 行降至 3,347 行。下一条合理边界是 7Z 原生解压，但审计确认
现有 7Z 路径仍有两项必须先修复的 P1 语义风险，不能只做机械搬迁。

## 2. 当前证据

| 项目 | 审计结果 |
| --- | --- |
| 版本一致性 | `package.json`、`Cargo.toml`、`tauri.conf.json` 和 Shell DLL 身份均为 1.0.19 |
| 正式发布 | v1.0.19 的 NSIS、updater ZIP、签名和 `latest.json` 均在线 |
| GitHub 状态 | 无开放 PR、无开放 Issue；本地与 `origin/master` 对齐 |
| 主线质量 | 最近 6 次 `master` CI 均成功；最新运行 `30503665108` 的 5 个 job 全部通过 |
| 分支保护 | 要求 PR、对话解决和严格状态检查；禁止强推与删除 `master` |
| Rust | 140 项单元测试及真实归档集成矩阵通过，Clippy `-D warnings` 通过 |
| 前端 | 30 个测试文件、182 项通过；25 项浏览器 E2E 通过 |
| 桌面端 | 严格真实全格式桌面矩阵通过；CI 可构建桌面 E2E 二进制和无签名 NSIS |
| 生产依赖 | `npm audit --omit=dev` 为 0 |
| 开发工具链 | 完整 npm 审计仍有 15 项，只能通过跨主版本升级处理 |

## 3. 代码与流程风险

### P1：7Z 错误分类仍可能误报密码

当前 `do_extract_7z` 通过错误文本包含 `password`、`Password` 或 `CRC` 来判断密码错误，并在没有提供密码时
返回“需要密码”。CRC 失败也可能来自未加密归档损坏，因此这里存在把损坏文件误导进密码字典流程的风险，
与此前已经修复的“未加密 ZIP 被报告为密码破解成功”属于同一类流程问题。

处理要求：

- 为未加密正常、未加密损坏、加密未提供密码、错误密码和正确密码建立独立真实样本回归；
- 优先使用 7Z 元数据或结构化错误分类，不能继续把所有 CRC 错误等同于密码错误；
- 密码判断失败时不启动字典攻击，不产生虚假的成功日志；
- 损坏归档应报告损坏或校验失败，并保持事务回滚。

### P1：7Z 时间戳选项未完整落地

当前 7Z 自定义写出路径没有把 `SevenZArchiveEntry` 的修改时间应用到输出文件。
这会使“保留时间戳”失效，也可能让“仅解压较新文件”拿暂存文件的当前时间与目标文件比较，从而错误覆盖。

处理要求：

- 分别验证 `preserve_timestamps` 开启和关闭；
- 为 `extract_only_newer` 建立“归档更旧、归档更新、时间相同”三类测试；
- 文件完全写入后再设置时间戳，失败时仍由事务层清理；
- ZIP、7Z、TAR 和通用引擎的选项语义应在能力矩阵中明确。

### P1：真实桌面测试尚未无人值守运行

交互式 Windows 本机已经通过完整真实矩阵，但 GitHub 托管 runner 无法稳定创建 WebView2 调试端口。
当前 CI 验证桌面测试二进制、脚本、驱动匹配和安装器构建，真实交互仍依赖发布前本机验收。

### P1：性能数据尚未形成趋势

100 MiB、1 GiB 和大量小文件基准已经存在，但默认忽略，尚未在固定机器积累足够样本。
不能用共享 runner 的单次数据设置硬阈值。

### P2：实验性并行解压器没有生产结论

`ParallelExtractor` 只在自身模块和测试中使用。它尚未对齐密码、冲突、时间戳、筛选、路径安全、
资源限制和事务回滚，因此不能直接接入生产，也不应长期保留为无主的第二套实现。

### P2：开发工具链需要独立升级

完整 npm 审计报告 15 项开发依赖漏洞，修复需要跨越 vue-tsc、Vite/Vitest 等主版本。
生产依赖为 0，因此应在独立迁移分支处理，不能与归档可靠性改动混在同一 PR。

### 外部约束

- Windows 11 顶层右键菜单和降低 SmartScreen 警告仍需要可信代码签名证书；
- HFSX 仍缺少可再分发、带真实载荷的稳定写入方案；
- 这些约束不阻断传统右键菜单和当前 v1.0.19 主流程。

## 4. 后续执行计划

### 阶段 D5：7Z 语义加固与引擎拆分

1. 先补密码、损坏、时间戳、筛选、路径、取消、跳过损坏和事务回滚测试；
2. 建立不会把普通 CRC/结构损坏误报为密码错误的分类边界；
3. 对齐 `preserve_timestamps` 和 `extract_only_newer`；
4. 将实现迁入 `native_extraction/seven_zip.rs`，通过 `ExtractionRuntime` 复用取消、日志、进度和缓冲池；
5. 保持格式路由、冲突决策和事务提交接口不变；
6. 运行 Rust 全目标、前端、浏览器 E2E、Shell Extension 和严格桌面全格式矩阵。

验收标准：

- 未加密损坏 7Z 永远不进入密码字典流程；
- 加密未提供密码、错误密码和正确密码三条路径结果准确；
- 时间戳和“仅解压较新文件”按选项生效；
- 取消、写满、筛选和损坏不留下已提交的部分输出；
- PR CI 与合并后 `master` CI 全绿。

### 阶段 D6：压缩写入边界与实验代码收口

1. 按 ZIP、7Z、TAR/AES 写入职责继续拆分 `compression_service.rs`，每种格式单独 PR；
2. 保持现有能力矩阵和动态引擎检测为唯一事实来源；
3. 完成 `ParallelExtractor` 生产语义评估：全部对齐后再接入，否则删除实验入口；
4. 不在本阶段混入 Tauri 2 或前端工具链主版本迁移。

### 阶段 Q：持续性能与桌面基础设施

1. 在固定 Windows 机器积累至少 10 次大文件、小文件和加密基准；
2. 基于稳定分布设置趋势告警，不以单次波动阻断发布；
3. 条件具备时接入交互式 self-hosted Windows runner，运行真实 WebView2 生命周期矩阵；
4. 每个正式 Release 继续执行独立公开应用内更新验收。

### 阶段 E：依赖与平台现代化

1. 先处理同主版本、低风险依赖；
2. Tauri 2、Vite/Vitest、vue-tsc 和 Pinia 主版本分别使用独立 PR；
3. 每个迁移必须重新验证安装、更新、托盘、右键菜单和归档闭环；
4. 获得可信证书后再启用 Windows 11 顶层菜单身份包。

## 5. 版本策略

当前继续保持 v1.0.19。本轮文档审计不创建新 Release。
完成阶段 D5、严格桌面回归和已安装环境验收后，再评估提升至 v1.0.20。
