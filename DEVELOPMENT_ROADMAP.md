# Long解压开发状态与路线图

> 审计日期：2026-08-01
> 当前正式版本：v1.0.21
> 默认分支：`master`
> 本轮详细审计：[DEVELOPMENT_AUDIT_2026-08-01.md](DEVELOPMENT_AUDIT_2026-08-01.md)

## 2026-08-01 发布后基线

v1.0.21 已完成正式发布。Release 四项资产、签名 updater、五组 CI 和 v1.0.20 → v1.0.21 真实 WebView2
公开更新均已验证；公开更新 21/21 项通过，传统右键菜单严格验证四类根和 17 条命令。Issue #39 与 #46
均已关闭，正式证据见 `long-compress-assistant/docs/RELEASE_VALIDATION_1.0.21.md`。

v1.0.21 标签之后只有验收脚本与文档变更，没有产品运行时代码差异，因此当前不创建空内容的 v1.0.22。
下一正式版本等待用户可感知的产品增量；当前路线转入 self-hosted 桌面 E2E、开发工具链分批主版本迁移、
HFSX/扩展格式真实样本和代码签名准备。

## 1. 当前结论

Long解压已经从“功能补齐”进入“核心流程稳定化、真实桌面验收和架构演进”阶段。

- v1.0.19 已正式发布；NSIS 安装器、updater ZIP、签名文件和 `latest.json` 均在线可用，公开 v1.0.18 → v1.0.19 应用内更新 18 项验收通过。
- PR [#13](https://github.com/Longyuyeee/long_Decompress/pull/13) 已合并，真实桌面生命周期发布门禁完成；
  前端、浏览器 E2E、Rust/Shell Extension、Windows 桌面构建和无签名 NSIS 安装包共 5 项 CI 全部通过。
- PR [#22](https://github.com/Longyuyeee/long_Decompress/pull/22) 已完成全格式真实样本实现与审计；严格桌面矩阵已验证
  25 种可创建格式，以及 QCOW2、VDI、VMDK、VHD/VHDX、FAT16、NTFS、APFS、SquashFS、MSI/MSM/MSP、
  UEFI、RAR、LHA、RPM、DMG/HFS 等只读格式的真实载荷。缺少任一生成器会使严格模式失败，不再静默跳过。
- 阶段 D 的首个架构边界已经完成：归档魔数/扩展名识别集中到 `archive_format.rs`，压缩能力、别名归一化、
  请求校验和执行路由集中到 `compression_format.rs`；原有公开服务接口保持兼容，
  `compression_service.rs` 从 4,281 行降至 3,961 行。本轮同时修复了声明别名没有全部进入正确执行路径，
  以及 `.tpz` 被当作普通 GZIP 单文件流的问题。
- 阶段 D 的第二个架构边界已经完成：暂存目录生命周期、资源限制、路径/reparse point 安全、
  冲突决策、事务提交和失败回滚集中到 `extraction_transaction.rs`，`compression_service.rs`
  进一步降至 3,613 行。暂存目录现在由 RAII 守卫保证所有提前返回路径均会清理，目录和文件都会计入条目上限，
  回滚不完整时也会返回可诊断错误。
- 阶段 D 的第三个架构边界已经完成：TAR、TAR.GZ/BZ2/XZ/Zstandard 与 GZ/BZ2/XZ/Zstandard
  单文件流原生解压实现迁入 `native_extraction/`，并通过窄化的 `ExtractionRuntime` 接口复用取消、
  进度和日志能力；`compression_service.rs` 降至 3,502 行。格式路由、密码处理和事务语义保持不变。
- 阶段 D 的第四个架构边界已经完成：ZIP 原生解压实现迁入 `native_extraction/zip.rs`，
  `compression_service.rs` 降至 3,347 行。密码预检现在扫描全部条目，能识别后置加密条目的混合 ZIP；
  取消和写入失败路径会显式归还 I/O 缓冲区，格式路由、事务提交和冲突策略保持不变。
- 阶段 D 的第五个架构边界已经完成：7Z 原生解压实现迁入 `native_extraction/seven_zip.rs`，
  `compression_service.rs` 降至 3,074 行。迁移前已修复 CRC/密码分类与时间戳语义，并以真实损坏包、
  加密包、过滤、取消、跳过损坏和暂存回滚回归保护生产行为。
- 阶段 D 的第六个架构边界已经完成：普通、AES-256 加密及分卷 ZIP 写入迁入
  `native_compression/zip.rs`，条目收集规则迁入 `compression_entries.rs`；公开路由、输出事务和取消语义
  保持不变，`compression_service.rs` 降至 2,977 行。154 项 Rust 测试、Clippy 和严格全格式桌面矩阵通过。
- 阶段 D 的第七个架构边界已经完成：普通及 AES 加密 7Z 写入迁入
  `native_compression/seven_zip.rs`，复用独立条目收集和窄化的 `CompressionRuntime`；字节进度、块级取消、
  输出事务和错误分类保持不变，`compression_service.rs` 降至 2,901 行。157 项 Rust 测试、Clippy 和
  重新构建后的严格全格式桌面矩阵通过。
- 阶段 D 的第八个架构边界已经完成：TAR/TAR.GZ/BZ2/XZ/Zstandard 写入迁入
  `native_compression/tar.rs`，GZ/BZ2/XZ/Zstandard/LZMA 单文件流写入迁入
  `native_compression/single_stream.rs`。密码回退与 AES 编排仍由核心层控制，统一条目收集、取消和进度
  语义保持不变；`compression_service.rs` 降至 2,526 行。158 项 Rust 测试、Clippy 和严格桌面矩阵通过。
- 阶段 D 的第九个及最后一个计划内架构边界已经完成：TAR.AES 与八种 `*.AES` 包装格式的写入编排迁入
  `native_compression/aes.rs`，临时输入由模块内 RAII 守卫清理，取消和加密失败仍保持原错误分类。
  `compression_service.rs` 降至 2,364 行；159 项 Rust 测试、Clippy、AES 字节往返和严格桌面矩阵通过。
- 压缩发布出口与 7Z 解压写入边界已经完成磁盘写满验收：所有非分卷压缩先写唯一临时输出，写满或发布失败时不会覆盖目标并会清理临时文件及旁车；
  7Z 解压使用可注入写入边界保留 `StorageFull` 类型，真实归档在中途写满时返回“磁盘空间不足”，删除半文件和暂存目录且不修改原目标。
  当前 161 项 Rust 测试、Clippy、重新构建的 Release Tauri 严格全格式桌面矩阵均通过。
- `1.0.20-rc.2` 已完成公开 v1.0.17 覆盖安装、卸载、用户数据保持和基线恢复 42 项检查；ZIP、7Z、7Z 快速解压、
  同源 ZIP → 7Z 原位替换和最终任务行界面均完成真实安装版验收。候选已由 PR #35 合入 `master`，进入正式 `1.0.20` 发布收口。
- 正式 `1.0.20` 版本身份、版本化 Shell DLL 和生产 NSIS 已收敛；v1.0.17 → v1.0.20 → 卸载 → v1.0.17 的
  42 项生命周期复验通过，原安装目录、传统菜单和两套用户数据指纹均恢复。
- `1.0.20-rc.1` 已完成不含 E2E 桥的生产 NSIS 构建及真实已安装环境闭环：从公开 `1.0.17` 覆盖安装到候选版，
  校验版本、原安装目录、版本化 Shell DLL、传统右键菜单和两套用户数据指纹后静默卸载，再恢复 `1.0.17`、
  原传统菜单注册和用户数据，共 42 项检查全部通过。验收脚本现在会备份并精确恢复应用自有注册表树，避免旧版启动时
  根据当前证书环境重新选择菜单模式。
- 2026-07-30 在已有旧 E2E 实例仍运行的情况下，桌面测试改为每次生成独立实例名、IPC socket、
  数据目录和 WebView2 用户目录；重新准备全部固定工具与样本后，严格全格式桌面矩阵及生命周期验收完整通过。
- v1.0.18 候选已完成公开 v1.0.17 → 候选版覆盖安装、传统菜单降级、历史 Shell DLL 清理、
  静默卸载、注册表清理、用户数据逐目录指纹保持和 v1.0.17 恢复，共 41 项真实已安装环境检查。
- v1.0.18 正式应用内更新验证确认签名下载和覆盖安装成功，同时发现被动更新后未自动重启；
  v1.0.19 已修复重启参数。公开 v1.0.18 → v1.0.19 的独立 WebView2 UI 更新验收 18 项全部通过。
- 核心压缩、解压、密码判断、密码候选、分卷、取消、事务式提交、托盘、传统右键菜单和应用内更新均有自动化回归。
- 2026-07-28 的真实使用反馈确认了同输出重复压缩、7Z 长时间无进度、压缩/解压任务串台和
  压缩中心任务生命周期缺失。阶段 1–4 已完成修复，并通过 24 MiB 真实 7Z 往返、
  96 MiB 真实取消及常用格式桌面矩阵；完整证据见
  [CORE_WORKFLOW_STABILIZATION.md](CORE_WORKFLOW_STABILIZATION.md)。
- 发布验收已形成版本化清单和 GitHub Issue 记录入口；`master` 已启用 PR、四项 CI、对话解决、禁止强推和禁止删除保护。
- 发布治理已完成历史收口：v1.0.14/v1.0.17 的旧跟踪项由 v1.0.18 覆盖安装验收和 v1.0.19 公开应用内更新证据取代并关闭；后续每个正式版本继续建立独立验收记录。
- Windows 11 顶层右键菜单和降低 SmartScreen 警告仍依赖可信代码签名证书；当前没有证书，因此暂缓。

## 2. 已达到的产品能力

### 2.1 归档核心

- 动态检测随包 7-Zip 引擎能力，避免界面声明与实际环境不一致。
- 支持 ZIP、7Z、RAR、TAR、GZ、BZ2、XZ、Zstandard、LZMA、WIM 及扩展镜像/容器格式。
- 支持密码 ZIP、7Z、RAR，以及项目自有 AES 容器。
- 未加密归档不会进入密码字典攻击，也不会产生虚假的“密码破解成功”。
- 解压使用暂存目录、资源限制、路径校验、冲突策略和事务式提交。
- 压缩与解压的大文件流式路径支持块级取消。

### 2.2 Windows 与交互

- 支持托盘运行、关闭时最小化、一键解压、一键 ZIP 打包和传统资源管理器右键菜单。
- Windows 11 顶层右键菜单扩展代码和测试身份包基础设施已经存在，但公开安装器会在没有可信证书时主动跳过身份包。
- 更新弹窗、活动任务保护、更新签名验证和失败重试已有前端回归。
- 配置组、密码保险箱、文件完整性和全局任务进度均已有关键交互测试。

### 2.3 发布与质量

- PR CI 包含前端类型检查、覆盖率、生产构建、Rust 全目标测试、Clippy、Shell Extension、Chromium E2E、
  Windows 桌面 E2E 构建和无签名 NSIS 构建。
- v1.0.19 Release 工作流已验证安装器、updater ZIP、`.sig` 和在线更新清单。
- 2026-07-30 本机复测 30 个测试文件、182 项全部通过；覆盖率为 75.22% 行/语句、78.03% 分支、63.68% 函数。
- Rust 归档测试覆盖真实格式矩阵、密码识别、分卷、事务回滚、路径安全和资源上限。

## 3. 已建立的性能基线

当前 Windows 测试机的 v1.0.13 Release 基线：

| 场景 | 压缩 | 解压 | 峰值工作集增量 |
| --- | ---: | ---: | ---: |
| 100 MiB 随机数据 | 48.30 MiB/s | 1301.19 MiB/s | 5.14 MiB |
| 1 GiB 随机数据 | 48.78 MiB/s | 1364.07 MiB/s | 2.40 MiB |
| 10,000 个 4 KiB 文件 | 3,235 文件/秒 | 4,660 文件/秒 | 2.96 MiB |

这些数据是同机回归参照，不是跨硬件的速度承诺。性能测试当前为显式运行的 ignored Rust 测试，还没有进入固定硬件的持续性能门。

## 4. 主要缺口与风险

| 优先级 | 事项 | 当前证据 | 处理原则 |
| --- | --- | --- | --- |
| 已完成 | 压缩/解压任务隔离 | 查询、启动、统计、清理和任务表均按类型过滤，已有前端回归 | 保持类型隔离回归 |
| 已完成 | 同一压缩输出并发保护 | 前端拒绝活动同输出，后端以规范化绝对路径原子占用 | 保持输出占用与释放回归 |
| 已完成 | 7Z 字节进度和可靠取消 | 元数据判断加密，压缩/解压按字节进度并在块边界取消 | 扩大到 1 GiB 压力矩阵 |
| 已完成 | 压缩中心任务生命周期 | 已展示状态、配置、进度、当前文件、日志、取消和独立清理 | 补充大规模视觉与压力验收 |
| 已完成 | `master` 分支保护 | 已要求 PR、四项 CI 和对话解决，并禁止强推和删除 | 持续核验 GitHub 配置；管理员紧急绕过必须留下记录 |
| 已完成 | 正式发布验收必须证明真实子菜单可用 | v1.0.20 → v1.0.21 公开更新 21/21 项通过，四类菜单根和 17 条命令均有严格证据 | 后续每个正式版本继续保留同等级公开更新验收 |
| 已完成 | AESENC01/TARAES01 整文件占用内存 | 新写入已切换到流式 `AESENC02/TARAES02`；v1 仅在 512 MiB 上限内只读兼容 | 保持 v1 兼容边界；在固定环境持续采集 v2 性能趋势 |
| P1 | 真实 Tauri 桌面 E2E 尚未进入无人值守运行 | Windows WebView2 启动、归档闭环、取消清理、退出确认、更新阻断和托盘恢复已在交互式 Windows 本机通过；GitHub CI 已验证相同 E2E 二进制、脚本和驱动匹配，但托管 runner 无法创建 WebView2 调试端口 | 有固定交互式 Windows 主机后接入 self-hosted runner；在此之前保留本机发布验收 |
| 已完成 | 前端覆盖率安全余量 | `CompressionView.vue` 的工具栏、状态进度和执行日志已拆为直接测试组件；整体行覆盖率 75.22% | 门槛已提高到行/语句 75%、分支 75%、函数 60%，持续防倒退 |
| P1 | 固定机器性能趋势仍需积累 | 结构化运行器已覆盖 ZIP 大文件/小文件、原生 7Z 和 AES v2，记录机器指纹、工具链、逐次样本和聚合值；少于 10 次或跨机器不会应用阈值 | 在固定 Windows 主机生成首份 10 次合格基线，再按版本保存同机比较证据并扩展格式场景 |
| 已完成 | 7Z 密码/损坏分类 | 已用真实未加密损坏包及真实加密包覆盖缺少、错误、正确密码；分类先依据加密元数据，普通 CRC/校验失败不再进入密码流程 | 拆分 7Z 引擎时保持这些回归 |
| 已完成 | 7Z 时间戳、事务与磁盘写满 | 文件完整写入后按 `preserve_timestamps` 或 `extract_only_newer` 恢复修改时间；归档更旧、相同、更新三态及过滤、取消、跳过损坏、暂存回滚均有真实回归；注入式真实 7Z 解压会在写入中途返回系统级 `StorageFull`，明确分类为磁盘空间不足并删除半文件和暂存目录 | 已随独立引擎迁移保持行为不变；继续保持故障注入和严格桌面矩阵回归 |
| 已完成 | 真实桌面生命周期自动化 | 真实可取消任务及残留清理、托盘隐藏/第二实例恢复、活动任务退出确认和更新阻断均已在逐次隔离的 Release Tauri 中通过 | 持续在交互式 Windows 运行，并接入 self-hosted 门禁 |
| P2 | 实验性并行解压器未进入生产 | `ParallelExtractor` 仅在自身模块和测试中使用 | 先对齐密码、冲突、时间戳、回滚和路径安全，再决定接入或删除 |
| 已完成 | 核心服务职责拆分 | 格式识别、路由、暂存、事务、原生解压及 ZIP/7Z/TAR/单文件流/AES 写入均已提取；`compression_service.rs` 从 4,281 行降至 2,364 行 | 后续只在有明确收益和完整矩阵保护时继续拆分 |
| P1 | 前端/桌面框架存在主版本迁移 | 生产依赖审计为 0；同主版本更新已完成，完整审计仍有 7 个开发工具链问题，Vite/Vitest/vue-tsc 等需要跨主版本 | 每个生态独立迁移和回归，禁止使用 `npm audit fix --force` 混入功能或 Release |
| 暂缓 | Windows 代码签名缺失 | 正式安装器跳过 Windows 11 身份包 | 当前不采购证书；继续交付传统菜单并保留身份包测试 |

## 5. 后续开发阶段

### 阶段 A：发布治理与可追溯验收

目标：先保证每次进入 `master` 和 Release 都有不可绕过的证据。

当前进度：分支保护、`RELEASE_CHECKLIST.md` 和发布验收 Issue 表单已建立；后续每个 Release 必须实际填写并回链验收记录。

1. 为 `master` 启用分支保护：
   - 要求最新 CI 成功；
   - 禁止强制推送和删除；
   - 要求通过 PR 合入；
   - 保留管理员紧急处理能力，但必须留下审计记录。
2. 建立 `RELEASE_CHECKLIST.md`：
   - 已安装应用的 ZIP/7Z/密码 ZIP/密码 7Z/RAR 解压；
   - 取消、磁盘不足、冲突策略和失败回滚；
   - 托盘、传统右键菜单、卸载；
   - 从上一正式版本执行应用内更新。
3. 将人工验收结果关联到 Release 或 GitHub Issue。

验收：无法在 CI 失败时合入 `master`；每个 Release 都能追溯自动化和人工验收结果。

### 阶段 B：AES 流式容器 v2

目标：大文件加密压缩和解压不再随文件大小线性占用内存。

当前进度：`AESENC02/TARAES02` 规范和统一分块内核已建立，新文件使用 v2，v1 保持 512 MiB 上限下的只读兼容；错误密码、篡改、截断、空文件、多分块、取消和磁盘写满清理已有回归。真实任务令牌已接入分块与 TAR 条目边界，100 MiB/1 GiB 峰值工作集基准均通过。阶段 B 的代码工作已完成，后续只需在固定环境持续积累性能趋势。

1. 先写容器规范：魔数、版本、分块大小、KDF 参数、每块 nonce/tag、尾部完整性和中断文件识别。
2. 保留 AESENC01/TARAES01 解密兼容，新文件使用明确的新版本魔数。
3. 对错误密码、篡改、中途取消、磁盘写满和截断文件增加真实回归。
4. 增加 100 MiB/1 GiB 加密基准和峰值工作集门槛。

验收：新格式峰值额外内存保持有界；旧格式仍可读取；错误密码、篡改、取消和磁盘写满不会留下已提交输出。

### 阶段 C：真实桌面 E2E 与关键覆盖率

目标：让自动化覆盖真实 Tauri 命令和 Windows 生命周期。

当前进度：已建立真实 Windows 桌面 E2E，通过 `tauri-driver` 和匹配 WebView2 Runtime
的 EdgeDriver 启动隔离的 Release Tauri 二进制，验证后端初始化、默认解压工作区和设置导航；
并通过第二实例桌面参数执行真实“一键 ZIP 打包 → 一键解压”闭环，验证非空归档及解压内容
逐字节一致，第二实例右键任务转发也已纳入自动化。
前端覆盖率已达到 75.22% 行/语句、78.03% 分支和 63.68% 函数；窗口原生控制具备可访问名称并有直接回归，
无障碍设置、解压磁盘失败和部分取消路径已纳入测试。
真实桌面生命周期已进一步覆盖：确定性长任务通过生产取消注册表停止并清理未完成输出；
原生退出判断会在活动任务存在且未启用关闭到托盘时显示三操作确认框；更新安装在任务运行时保持禁用；
托盘路径由 Tauri 自身可见性状态验证隐藏，并由第二实例 IPC 验证恢复。
交互式 Windows 本机已通过。GitHub 托管 Windows runner 无法创建 WebView2 调试端口，因此 CI
当前只执行 `Windows desktop E2E build`，验证隔离二进制、脚本和驱动匹配，安装包 job 依赖该门禁。
下一步只需在具备固定交互式 Windows 主机时接入 self-hosted runner，再补文件选择与拖放夹具；
这属于测试基础设施增强，不是当前正式版本的代码故障。

1. 建立可启动真实桌面后端的 Windows E2E。
2. 优先覆盖：
   - 应用启动与单实例；
   - 文件选择/拖放后的真实任务；
   - 取消与退出编排；
   - 关闭最小化到托盘；
   - 右键任务队列；
   - 更新期间的任务阻断。
3. `useTauriCommands`、`WindowTitleBar`、无障碍设置，以及解压磁盘失败和部分取消路径已完成首批覆盖。

验收：关键桌面路径不再只依赖 Mock；覆盖率门槛固定为行/语句 75%、分支 75%、函数 60%。

### 阶段 D：性能持续化与架构拆分

目标：让性能变化可长期比较，并降低核心服务修改风险。

当前进度：结构化性能运行器已覆盖大文件 ZIP、小文件 ZIP、原生 7Z 和 AES v2 真实往返，记录机器指纹、活动电源计划、
Git/工具链、逐次样本及中位数区间；只有固定机器至少 10 次的基线才能启用回归阈值，单次共享 runner 数据不会阻断发布。
格式路由/能力判断以及暂存/资源限制/事务提交两个边界均已完成拆分。所有能力矩阵中声明的扩展名
都必须通过统一归一化和公开服务门面测试；`.tpz` 等 TAR 包装别名也由单一规则识别。事务模块新增暂存 RAII 清理、
目录条目计数、Windows reparse point 防护和回滚失败诊断。完整 Rust 全目标回归、Clippy、182 项前端测试、
25 项浏览器 E2E、Shell Extension 测试及严格真实 Windows 桌面全格式矩阵均已通过。
TAR 系列、单文件流、ZIP 和 7Z 原生解压已经迁入 `native_extraction/`。ZIP 拆分同步补齐了全条目密码预检
和异常路径缓冲区归还回归；7Z 拆分同步补齐了 CRC/密码分类与时间戳选项回归。普通、AES-256 加密及分卷
ZIP、7Z、TAR 系列、单文件流与项目自有 AES 写入均已迁入 `native_compression/`，并通过严格全格式
桌面矩阵。阶段 D 的计划内职责拆分、可注入磁盘写满和已安装环境验收已经收口；后续只保留固定环境性能趋势积累。

1. 在固定 Windows 环境周期运行 `npm.cmd run performance:baseline`，保存大文件、小文件和 AES v2 的结构化结果。
2. 首份基线累积至少 10 次稳定样本后，再用 `-BaselinePath` 设置同机告警阈值；跨机器或不足样本会拒绝/跳过门禁。
3. 拆分 `compression_service.rs`：
   - 格式路由与能力判断；
   - ZIP/7Z/TAR/单文件流解压实现（已完成）；
   - ZIP/7Z/TAR/单文件流压缩写入实现（已完成）；
   - 项目自有 AES 容器编排（已完成）；
   - 暂存、资源限制和事务提交；
   - 密码验证与加密容器。
4. 对 `ParallelExtractor` 做生产语义评估；不能完整对齐时删除实验入口，避免长期双实现。

验收：核心模块职责清晰；格式回归不减少；性能趋势可在版本间比较。

### 阶段 E：依赖与平台现代化

目标：在稳定主线之外完成框架迁移。

当前进度：同主版本低风险更新已完成首批验证，Vue 运行时及 compiler/runtime overrides 统一为 3.5.40，
Playwright 更新到 1.62.1，并同步更新 Vue Test Utils、GSAP、PostCSS 和 Autoprefixer。干净 `npm ci`、
184 项覆盖率测试、25 项多浏览器 E2E、类型检查、生产构建和版本身份均通过；生产依赖漏洞仍为 0。
剩余 15 项全部位于开发工具链，且 npm 提供的修复需要 Vite/Vitest/vue-tsc 等跨主版本升级，因此继续分批处理。

1. 先处理同主版本、低风险依赖更新（首批已完成）。
2. 分别评估 Tauri 2、Vite/Vitest 新主版本、Pinia 新主版本和测试工具升级。
3. 每个主版本迁移单独 PR，并要求安装器、更新、托盘和右键菜单回归。
4. 获得可信代码签名证书后，启用 Windows 11 顶层右键菜单身份包并验证 SmartScreen/升级/卸载。

验收：迁移不改变归档语义；公开安装包具备稳定签名身份；Windows 11 顶层菜单可正式交付。

## 6. 暂不执行的事项

- 不直接启用当前实验性并行解压器。
- 不原地修改 AESENC01/TARAES01 格式。
- 不把开发机单次性能数据作为跨机器硬阈值。
- 不在可靠性修复中混入全仓 Rust 格式化或框架主版本迁移。
- 不在没有可信证书时向正式安装包注入 Windows 11 身份包。

## 7. 下一阶段推荐顺序

1. 合并 v1.0.21 发布提交并创建正式 tag/Release；随后从保留的公开 v1.0.20 环境运行 `test:public-update`，用 17 条菜单证据关闭 Issue #39。
2. 用已落地的结构化运行器在固定 Windows 环境积累首份至少 10 次的大文件 ZIP、小文件 ZIP、原生 7Z 与 AES v2 基线；后续扩展格式按真实样本价值逐批补入。
3. 准备 self-hosted Windows runner，把当前本机严格桌面矩阵迁入可值守环境；PR 保留快速门禁，Release 前运行完整矩阵。
4. 继续寻找可再分发的非空 HFSX 样本或可靠写入工具；在真实载荷闭环前不把 HFSX 标记为完整支持。
5. Vite/Vitest/vue-tsc、Tauri 2 和 Windows 11 顶层菜单/代码签名分别独立评估，不与归档语义修复混合。
