# Long解压发布验收清单

本清单用于正式版本发布前后的 Windows 已安装应用验收。每次发布都必须创建一条
[发布验收记录](https://github.com/Longyuyeee/long_Decompress/issues/new?template=release-validation.yml)，
填写实际结果并在 Release 说明中链接该记录。

## 1. 验收记录

| 项目 | 结果 |
| --- | --- |
| 候选版本 | `vX.Y.Z` |
| 候选提交 | `<commit SHA>` |
| 上一正式版本 | `vX.Y.Z` |
| CI 运行 | `<GitHub Actions URL>` |
| Release 运行 | `<GitHub Actions URL>` |
| 安装器 SHA-256 | `<sha256>` |
| Windows 版本 | `<edition / version / build>` |
| 验收人 | `<GitHub 用户名>` |
| 验收时间 | `<YYYY-MM-DD HH:mm TZ>` |

结果标记：

- `PASS`：符合预期并附有必要证据。
- `FAIL`：不符合预期；必须链接阻断 Issue，不能发布。
- `N/A`：当前版本不适用；必须写明原因。

## 2. 自动化门禁

- [ ] 正式版本号未用于另一个已安装、已交付或已公开分发的构建。
- [ ] 开发构建使用 `-dev`，候选构建使用 `-rc.N`，正式版本号只对应最终发布提交。
- [ ] PR 的 `Frontend checks` 通过。
- [ ] PR 的 `Rust and shell-extension checks` 通过。
- [ ] PR 的 `Browser shell E2E` 通过。
- [ ] PR 的 `Windows desktop E2E build` 通过。
- [ ] PR 的 `Windows installer` 通过。
- [ ] 本地严格桌面 E2E 使用 `VITE_DESKTOP_E2E=1` 与 `desktop-e2e` feature 的隔离构建通过；
      随后重新执行不含测试开关的生产打包，并仅以重建后的安装包作为候选产物。
- [ ] 版本号已在 `package.json`、`package-lock.json`、主程序与 Shell Extension
      `Cargo.toml`、Cargo lockfile 和 `tauri.conf.json` 中保持一致。
- [ ] Git 标签、应用版本、Release 名称、安装包名称和 `latest.json` 版本完全一致。
- [ ] Release notes 已描述用户可见变化、修复和已知限制。
- [ ] 候选提交位于受保护的 `master`，且本地工作区干净。

## 3. 安装、升级和卸载

- [ ] 在未安装 Long解压的 Windows 11 环境完成全新安装。
- [ ] 开始菜单、桌面入口、窗口标题、左上角图标和托盘名称均显示“Long解压”。
- [ ] 从上一正式版本执行覆盖安装，设置和密码保险箱数据保持不变。
- [ ] 正式 Release 发布后，从仍保留的上一正式版本执行应用内更新，下载、校验、安装和重启流程正常。
- [ ] 使用 `test:public-update` 保存真实 UI 更新证据，确认版本、原安装目录、用户数据指纹、
      自动重启、单一 Shell DLL、传统右键菜单和无签名 MSIX 残留检查全部通过。
- [ ] 卸载后主程序、开始菜单项和传统资源管理器右键菜单被清理。
- [ ] 卸载不会误删用户主动选择保留的数据。

## 4. 压缩与解压矩阵

测试文件至少包含一个普通文件、一个空文件、一个子目录和一个非 ASCII 文件名。
每次解压后应核对目录结构、文件大小和内容哈希。

| 场景 | 预期 | 结果 |
| --- | --- | --- |
| ZIP 压缩与解压 | 内容和目录结构一致 | |
| 7Z 压缩与解压 | 内容和目录结构一致 | |
| 密码 ZIP 解压 | 正确密码成功，错误密码不提交输出 | |
| 密码 7Z 解压 | 正确密码成功，错误密码不提交输出 | |
| RAR 解压 | 内容和目录结构一致 | |
| 多文件一键 ZIP 打包 | 在当前目录生成同名或指定名称压缩包 | |
| 一键解压 | 自动解压到同名文件夹 | |
| 分卷归档 | 能识别首卷并完整还原 | |

## 5. 失败、取消与事务安全

- [ ] 压缩过程中取消，界面及时停止且不留下可误认为成功的成品。
- [ ] 解压过程中取消，暂存输出被清理，原目标目录不被破坏。
- [ ] 目标文件已存在时，覆盖、跳过和重命名策略分别符合选择。
- [ ] 模拟目标磁盘空间不足，任务明确失败且不提交半成品。
- [ ] 损坏或截断归档返回可理解的错误，不崩溃。
- [ ] 路径穿越条目和资源超限归档被拒绝。
- [ ] 应用有进行中任务时，退出和安装更新的阻断行为正确。

## 6. 桌面与资源管理器集成

- [ ] 关闭主窗口后按设置最小化到托盘。
- [ ] 托盘悬停显示“Long解压”。
- [ ] 托盘菜单的显示主窗口、一键操作和退出功能正常。
- [ ] Windows 11“显示更多选项”中的传统右键菜单正常。
- [ ] 右键“一键解压”自动创建同名文件夹。
- [ ] 多选文件后右键“一键打包”在当前目录生成 ZIP。
- [ ] `npm.cmd run test:context-menu-package` 在管理员 PowerShell 中通过。
- [ ] Windows 11 顶层右键菜单标记为 `N/A`：当前无可信代码签名证书，正式安装器
      主动跳过身份包。

## 7. 密码保险箱与界面回归

- [ ] 首次进入密码保险箱时显示加载状态，不先闪烁空数据。
- [ ] 创建、编辑、复制、显示/隐藏和删除密码均正常。
- [ ] 密码名称、密码正文和备注区域布局清晰。
- [ ] 解压中心拒绝 DOCX、XLSX 等不作为归档导入的普通文档。
- [ ] 全局任务统计不重叠，进行中数量表达唯一且清晰。
- [ ] 主题强调色、开关选中和未选中状态有明显区别。

## 8. 发布资产核验

- [ ] Release 中存在 `Long-Decompress_<version>_x64-setup.exe`。
- [ ] Release 中存在 `.nsis.zip`、`.nsis.zip.sig` 和 `latest.json`。
- [ ] `latest.json` 的版本、下载地址和签名与当前 Release 一致。
- [ ] `latest.json` 的版本严格高于上一正式安装版本，能够触发 updater 的语义版本比较。
- [ ] 在干净环境下载并安装公开 Release 资产，而不是本地构建产物。
- [ ] 安装器 SHA-256 已写入发布验收记录。
- [ ] Release 说明已链接发布验收记录，并明确无商业代码签名及 SmartScreen 提示。

## 9. 发布判定

当前公开版 updater 固定读取 GitHub 的“最新正式 Release”。草稿和预发布不会出现在该端点，
因此从上一正式版到候选版的应用内更新无法在正式发布前安全验证；不得通过替换上一版本
`latest.json` 的方式把候选更新暴露给现有用户。

发布采用两段式判定：

1. 发布前必须完成版本一致性、CI、全新/覆盖安装、右键菜单、卸载、用户数据保持和核心归档矩阵。
2. 正式 Release 生成后立即保留一台上一正式版环境执行应用内更新，并把结果回填验收记录。
3. 发布后应用内更新失败视为发布事故：停止继续推广、创建阻断 Issue，并在修复版中重新执行完整验收。

只有以下条件同时满足时才能最终标记验收通过：

1. 所有必需项均为 `PASS`。
2. 所有 `N/A` 均有明确理由。
3. 没有未解决的发布阻断 Issue。
4. 自动化运行、人工验收记录和最终 Release 可以相互追溯。

发现失败项时，不得通过重复尝试掩盖问题。应保存日志或截图、创建阻断 Issue，
修复后使用新的候选提交重新执行受影响项目。
