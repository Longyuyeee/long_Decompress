# Long-Decompress 功能开发规划

> **产品定位**: 全能格式 × 智能密码 × 批量自动化 × 华丽 UI  
> **开发推进**: Claude AI  
> **目标版本**: v1.0.0

---

## 🎯 核心差异化能力

### 1. 格式覆盖率最广
- ✅ **已实现**: 37+ 解压格式, 16 种压缩格式
- 支持主流格式: ZIP, 7Z, RAR, TAR, GZ, BZ2, XZ, Zstd
- 支持特殊格式: ISO, IMG, WIM, VHD, DEB, RPM, DMG
- 密码压缩: ZIP(pwd), 7Z(pwd), RAR(pwd)
- 分卷压缩: ZIP 分卷支持

### 2. 密码智能匹配
- ✅ **已实现**: 密码保险箱加密存储, 基础自动尝试
- 🔄 **开发中**: 优先级匹配, 智能规则生成
- ⏳ **待实现**: 实时进度显示, 策略配置

### 3. 批量工业化
- ✅ **已实现**: 任务队列系统, 批量任务执行
- 🔄 **开发中**: 配置组模板系统
- ⏳ **待实现**: 拖拽应用, 自动统计

### 4. UI 体验领先
- ✅ **已实现**: 13 种主题, 玻璃拟态设计, 基础动效
- 🔄 **开发中**: 动画系统增强
- ⏳ **待实现**: 微交互打磨

---

## 📋 模块开发状态

### 模块 A: 配置组模板系统 🔥 [优先级: P0]

**目标**: 用户可创建/保存/应用可复用的压缩配置模板

#### A1. 数据模型层
- ✅ **已完成** - `models/compression_profile.rs`
  - CompressionProfile 结构体（id, name, icon, description, config, auto_apply, password_strategy, stats）
  - CompressionConfig（format, level, password, split, solid_archive, filename_template）
  - AutoApplyRule（模式匹配、文件类型过滤、大小范围）
  - PasswordStrategy（None/Fixed/FromVault/AutoGenerate）
  - 5 个内置默认配置组定义

#### A2. 服务层
- ✅ **已完成** - `services/compression_profile_service.rs`
  - 完整 CRUD 实现（创建、读取、更新、删除）
  - 初始化默认配置组到数据库（5 个内置配置组）
  - 统计信息更新（使用次数、成功率、处理量）
  - 自动匹配逻辑（根据文件类型/大小推荐配置组，按成功率/使用次数排序）
  - 配置组排序持久化（display_order 字段）
  - SQLite 数据库持久化（完全替代 JSON 文件方案）

#### A3. Tauri 命令层
- ✅ **已完成** - `commands/compression_profile.rs`
  - 8 个 Tauri 命令完整实现（get/create/update/delete/reorder/apply/suggest）
  - 已连接到服务层
  - 错误处理完善（统一 Result<T, String> 错误格式）
  - 已在 main.rs 中注册所有命令
  - 服务状态管理（CompressionProfileServiceState + Arc<Mutex>）
  - 应用启动时自动初始化默认配置组

#### A4. 前端状态管理
- ✅ **已完成** - `stores/compressionProfile.ts`
  - 完整 CRUD 操作封装（loadAllProfiles, addProfile, modifyProfile, removeProfile）
  - 配置组排序逻辑（按最后使用时间、使用次数排序）
  - 统计信息实时更新（applyProfile 后自动重载）
  - 智能推荐接口（getSuggestedProfile）
  - 错误处理和加载状态管理
- ✅ **已完成** - `composables/useCompressionProfiles.ts`
  - 所有 Tauri 命令调用已对齐后端 API
  - 统一错误处理和日志记录

#### A5. 前端组件
- ✅ **已完成** - `components/profiles/ProfileSelector.vue` (基础框架)
- ⏳ **待实现**:
  - [ ] **ProfileSelector** - 配置组卡片网格选择器
    - 卡片布局（图标 + 名称 + 描述 + 统计徽章）
    - 快捷应用按钮
    - 拖拽排序
    - 悬浮预览详细配置
  - [ ] **ProfileManager** - 配置组管理界面
    - 左侧列表（可搜索、可拖拽排序）
    - 右侧详细编辑面板（所有参数可调）
    - 自动应用规则配置 UI
  - [ ] **增强 CompressionSettingsPanel**
    - 顶部添加"选择配置组"快捷按钮
    - 应用配置组后自动填充参数
    - "保存为新配置组"按钮

#### A6. 视觉交互
- ⏳ **待实现**:
  - [ ] 配置组卡片玻璃拟态样式
  - [ ] 拖拽视觉反馈（半透明 + 虚线框）
  - [ ] 统计徽章设计（使用次数、成功率）
  - [ ] 拖拽配置组到文件/任务的应用逻辑

**依赖**: 无  
**阻塞**: 模块 B（密码智能匹配需要配置组中的 PasswordStrategy）

---

### 模块 B: 密码智能匹配增强 ⚠️ [优先级: P0]

**目标**: 自动尝试密码本中的密码，支持优先级排序和规则生成

#### B1. 后端优化
- ✅ **已实现** - `services/password_attempt_service.rs`
  - 基础自动尝试流程
- ⏳ **待实现**:
  - [ ] 验证现有流程完整性（测试自动触发）
  - [ ] **优先级策略**:
    - 按使用频率排序（最近成功的密码优先）
    - 按分类匹配（文件名关联的分类优先）
    - 按密码复杂度（简单 → 复杂）
  - [ ] **密码规则生成器** - `services/password_rule_generator.rs`
    - 基于文件名的规则（filename + 后缀）
    - 基于日期的规则（YYYYMMDD 组合）
    - 常用模式库（123456, password123 等）

#### B2. 前端集成
- ⏳ **待实现**:
  - [ ] **密码尝试进度显示** - `components/passwords/PasswordAttemptProgress.vue`
    - 实时显示："正在尝试密码本中的 3/12 个密码..."
    - 进度条 + 当前尝试的密码提示（脱敏显示）
    - 失败后提示手动输入
  - [ ] **密码匹配策略选择** - 设置面板
    - 启用/禁用自动尝试
    - 最大尝试次数（默认 20）
    - 失败后行为（停止/继续/提示）

**依赖**: 模块 A（配置组中需要 PasswordStrategy 配置）  
**阻塞**: 无

---

### 模块 C: 密码库导入导出前端连接 🔐 [优先级: P1]

**目标**: 完整的密码导入导出用户界面

#### C1. 后端验证
- ✅ **已完成**:
  - `commands/encrypted_password.rs` - `export_passwords_command`
  - `commands/encrypted_password.rs` - `import_passwords_command`
  - `composables/useTauriCommands.ts` - 命令封装

#### C2. 前端补全
- ⏳ **待实现**:
  - [ ] **导出功能** - `views/PasswordVaultView.vue`
    - 导出格式选择（JSON 加密 / JSON 明文 / CSV）
    - 加密选项（密码保护导出文件）
    - 保存位置选择
    - 导出进度提示
  - [ ] **导入功能**
    - 文件选择（支持拖拽）
    - 格式自动识别
    - 冲突处理策略（跳过/覆盖/合并）
    - 导入预览（显示将要导入的条目数）
  - [ ] **导入导出模态框** - `components/passwords/ImportExportModal.vue`
    - 统一入口，选项卡切换导入/导出
    - 历史记录（最近导入导出的文件）

**依赖**: 无  
**阻塞**: 无

---

### 模块 D: 安全修复 - 密码暴露 🔴 [优先级: P0]

**目标**: 确保密码不出现在进程列表和日志中

#### D1. 代码审计
- ⏳ **待实现**:
  - [ ] 扫描所有 CLI 调用（搜索 `-p` 参数）
    - `services/universal_engine.rs` - 7z（✅ 已修复，使用环境变量）
    - `services/rar_support.rs` - RAR/unrar（需验证）
    - `services/compression_service.rs` - 检查所有外部命令
  - [ ] **修复方案**:
    - 7z: 使用 `_7ZIP_PASSWORD` 环境变量 ✅
    - RAR: 优先使用原生 `unrar` crate，避免 CLI
    - WinRAR: 使用临时文件传密码（`-p@password.txt`）

#### D2. 测试验证
- ⏳ **待实现**:
  - [ ] 进程列表测试（确认密码不可见）
  - [ ] 日志脱敏检查（所有日志中密码已脱敏为 `***`）
  - [ ] 错误堆栈检查（无密码泄露）

**依赖**: 无  
**阻塞**: 发布（P0 必须完成）

---

### 模块 E: UI 动画系统 🎬 [优先级: P1]

**目标**: 统一的动画系统，支持 3 种风格 × 4 档强度

#### E1. 动画引擎
- ⏳ **待实现**:
  - [ ] **动画配置** - `utils/animations.ts`
    - 3 种风格（elegant/bouncy/snappy）
    - 4 档强度（off/reduced/normal/enhanced）
    - 自动降级（检测低配设备）
  - [ ] **动画 Composable** - `composables/useAnimation.ts`
    - 读取用户设置
    - 动画类名生成器

#### E2. 核心动画实现
- ⏳ **待实现**:
  - [ ] **任务列表动画** - `components/tasks/AeroTable.vue`
    - 进入动画：stagger 错开（每项延迟 50ms）
    - 进度条：渐变流动（shimmer 效果）
    - 完成动画：绿色波纹扩散
  - [ ] **模态框动画**
    - 弹出：从中心缩放 + 淡入
    - 关闭：缩小 + 淡出
    - 背景：模糊渐入
  - [ ] **按钮微交互**
    - 悬浮：轻微上移 + 阴影加深
    - 按下：scale-95 + 阴影减弱
    - 点击：涟漪效果（ripple）

#### E3. 设置界面
- ⏳ **待实现**:
  - [ ] 动画控制面板（风格选择器 + 强度滑块）
  - [ ] 实时预览区域（点击触发动画演示）

**依赖**: 无  
**阻塞**: 无

---

### 模块 F: 主题系统扩展 🎨 [优先级: P2]

**目标**: 扩展到 13 种主题 + 主题选择器优化

#### F1. 新增主题
- ✅ **已实现**: 5 种基础主题
- ⏳ **待实现**:
  - [ ] Nord (极地冰原)
  - [ ] Dracula (德古拉夜)
  - [ ] Solarized Light (阳光护眼)
  - [ ] Tokyo Night (东京之夜)
  - [ ] Monokai (经典编辑器风)
  - [ ] Gruvbox (复古暖调)
  - [ ] One Dark (Atom 风格)
  - [ ] Material Ocean (海洋蓝调)

#### F2. 主题选择器
- ⏳ **待实现**:
  - [ ] **华丽主题网格** - `components/settings/ThemeSelector.vue`
    - 4 列网格布局
    - 每个主题卡片（颜色预览条 + 名称 + 选中标记）
    - 悬浮放大（scale-105）
    - 点击平滑过渡（0.7s）
  - [ ] "跟随系统"选项（检测 Windows/macOS 系统主题）

**依赖**: 无  
**阻塞**: 无

---

### 模块 G: UI 组件美化 ✨ [优先级: P2]

**目标**: 提升各组件视觉效果和交互体验

#### G1. 格式选择器重构
- ⏳ **待实现**:
  - [ ] **华丽网格卡片** - `CompressionSettingsPanel.vue`
    - 从按钮升级为卡片网格
    - 每个格式卡片（大图标 + 格式名 + 能力标签）
    - 悬浮动效（旋转 + 放大）
    - 选中光晕（渐变背景 + 脉冲）

#### G2. 任务列表增强
- ⏳ **待实现**:
  - [ ] **AeroTable 动效升级**
    - 进入动画（从底部滑入）
    - 进度条渐变（彩虹流动）
    - 状态指示器动画（等待脉冲/进行中转圈/完成弹跳/错误抖动）

#### G3. 文件拖放区美化
- ⏳ **待实现**:
  - [ ] **EnhancedFileDropzone 增强**
    - 拖入时背景辉光效果
    - 文件图标悬浮动画
    - 支持格式滚动提示（跑马灯）
    - 拖放预览（显示即将添加的文件数）

#### G4. 细节优化
- ⏳ **待实现**:
  - [ ] 输入框聚焦效果（边框发光）
  - [ ] 勾选框动画（对勾路径动画）
  - [ ] 下拉菜单动画（滑入/淡入）
  - [ ] Tooltip 提示（延迟显示 + 淡入淡出）

#### G5. 空状态设计
- ⏳ **待实现**:
  - [ ] 无任务时华丽插画（SVG 动画）
  - [ ] 无密码时引导添加

#### G6. 加载状态
- ⏳ **待实现**:
  - [ ] **骨架屏** - `components/ui/Skeleton.vue`
    - 替代全局转圈圈
    - shimmer 动画

**依赖**: 模块 E（动画系统）  
**阻塞**: 无

---

### 模块 H: 设置面板完善 ⚙️ [优先级: P2]

**目标**: 补全设置项，增强输入验证

#### H1. 表单验证
- ⏳ **待实现**:
  - [ ] 线程数：1-32，默认 CPU 核心数
  - [ ] 缓存大小：100-10240 MB，默认 512
  - [ ] 默认压缩级别：0-9
  - [ ] 分卷大小：1-999999 MB

#### H2. 快捷键配置
- ⏳ **待实现**:
  - [ ] 全局快捷键（压缩/解压）
  - [ ] 配置组快捷键（Ctrl+1~9）

**依赖**: 无  
**阻塞**: 无

---


## 📊 质量指标

### 代码质量
- ⏳ Clippy 警告 < 50（当前 64，已从 129 降至 64）
- ✅ P0 安全问题（Zip Slip 防护、主密钥随机化、密码脱敏）
- ⏳ 核心测试覆盖率 > 80%（当前 35 个测试通过）

### 性能指标
- ⏳ 启动时间 < 2s
- ⏳ 压缩性能：ZIP-L6 约 50MB/s
- ⏳ UI 响应 < 100ms

### 用户体验
- ⏳ 所有主要操作 < 3 步完成
- ✅ 错误提示清晰（已修复 [object Object] 问题）
- ⏳ 无闪烁、无卡顿

---

## 🚀 发布检查清单

### P0 - 必须完成
- [ ] 安装器生成（WiX/DMG/deb/rpm）
- [ ] 模块 D: 密码不暴露在进程列表 ⚠️
- [ ] 模块 A: 配置组系统核心功能可用
- [ ] 崩溃 Bug 清零

### P1 - 强烈建议
- [ ] 模块 A: 配置组系统完整可用
- [ ] 模块 B: 密码智能匹配增强
- [ ] 模块 C: 密码库导入导出连接
- [ ] 模块 E: 动画系统完整

### P2 - 可延后
- [ ] 模块 F: 13 种主题实现
- [ ] 模块 G: UI 组件美化
- [ ] 模块 H: 设置面板完善
- [ ] CI/CD 自动构建
- [ ] 11 个旧测试修复
- [ ] 英文文档

---

## 📝 已知技术债务

### 不阻塞发布
1. Clippy 警告（64 个，主要是代码风格）
2. 旧测试文件需要迁移（11 个）
3. 数据库无版本迁移系统
4. Toast 通知系统未统一

### 未来改进
1. 自动更新系统（签名密钥待配置）
2. 云端密码本同步
3. 插件系统（自定义格式支持）
4. AI 压缩参数推荐

---

## 🔄 开发流程规则

### 开发铁律
1. **先计划再动手**: 任何改动前先列出详细计划
2. **每步必测**: 每完成一个独立改动，立即编译验证（`cargo check` + `vue-tsc`）
3. **每步必提交**: 每个独立功能点完成后立即 commit + push
4. **不影响现有功能**: 改动范围精确控制
5. **增量修改优先**: 用 Edit 工具精确替换
6. **真实数据验证**: 编译通过是底线，零错误零警告才是完成

### 提交规范
- 每个 commit 只做一件事
- commit message 使用英文，格式：`type: description`（feat/fix/refactor/chore）
- 推送前确认编译通过
- Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

---

**项目负责人**: Claude AI  
**最后更新**: 2026-07-13  
**目标版本**: v1.0.0
