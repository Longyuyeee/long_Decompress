# 胧解压·方便助手 - 设计规范

## 1. 设计系统

### 1.1 颜色规范

#### 主色调
```css
/* 品牌色 */
--color-primary: #3498db;      /* 主要按钮、重要操作 */
--color-primary-dark: #2980b9; /* 悬停状态 */
--color-primary-light: #5dade2; /* 激活状态 */

/* 功能色 */
--color-success: #2ecc71;      /* 成功状态 */
--color-warning: #f39c12;      /* 警告状态 */
--color-error: #e74c3c;        /* 错误状态 */
--color-info: #3498db;         /* 信息提示 */

/* 中性色 */
--color-gray-50: #f8f9fa;      /* 背景最浅 */
--color-gray-100: #e9ecef;     /* 背景浅 */
--color-gray-200: #dee2e6;     /* 边框浅 */
--color-gray-300: #ced4da;     /* 边框 */
--color-gray-400: #adb5bd;     /* 文字辅助 */
--color-gray-500: #6c757d;     /* 文字次要 */
--color-gray-600: #495057;     /* 文字主要 */
--color-gray-700: #343a40;     /* 文字强调 */
--color-gray-800: #212529;     /* 文字重要 */
--color-gray-900: #121416;     /* 背景深 */
```

#### 语义颜色映射
```css
/* 状态颜色 */
.status-waiting { color: var(--color-gray-500); }
.status-processing { color: var(--color-primary); }
.status-success { color: var(--color-success); }
.status-warning { color: var(--color-warning); }
.status-error { color: var(--color-error); }

/* 进度条颜色 */
.progress-bar { background: var(--color-primary); }
.progress-bar-success { background: var(--color-success); }
.progress-bar-warning { background: var(--color-warning); }
.progress-bar-error { background: var(--color-error); }
```

### 1.2 字体规范

#### 字体家族
```css
/* 主要字体 */
--font-family-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
                    "Helvetica Neue", Arial, "Noto Sans", sans-serif;

/* 等宽字体 */
--font-family-mono: "SF Mono", Monaco, "Cascadia Mono", "Roboto Mono",
                    "Courier New", monospace;

/* 中文优化 */
--font-family-cn: "Microsoft YaHei", "PingFang SC", "Hiragino Sans GB",
                  "WenQuanYi Micro Hei", sans-serif;
```

#### 字体大小
```css
/* 基础大小 */
--font-size-base: 16px;

/* 标题层级 */
--font-size-h1: 2.5rem;    /* 40px */
--font-size-h2: 2rem;      /* 32px */
--font-size-h3: 1.75rem;   /* 28px */
--font-size-h4: 1.5rem;    /* 24px */
--font-size-h5: 1.25rem;   /* 20px */
--font-size-h6: 1rem;      /* 16px */

/* 正文层级 */
--font-size-lg: 1.125rem;  /* 18px */
--font-size-md: 1rem;      /* 16px */
--font-size-sm: 0.875rem;  /* 14px */
--font-size-xs: 0.75rem;   /* 12px */
```

#### 行高规范
```css
--line-height-tight: 1.2;      /* 标题 */
--line-height-normal: 1.5;     /* 正文 */
--line-height-loose: 1.8;      /* 长文本 */
```

### 1.3 间距规范

#### 间距单位
```css
/* 基础单位 */
--spacing-unit: 8px;

/* 间距尺度 */
--spacing-xxs: calc(var(--spacing-unit) * 0.5);  /* 4px */
--spacing-xs: var(--spacing-unit);               /* 8px */
--spacing-sm: calc(var(--spacing-unit) * 1.5);   /* 12px */
--spacing-md: calc(var(--spacing-unit) * 2);     /* 16px */
--spacing-lg: calc(var(--spacing-unit) * 3);     /* 24px */
--spacing-xl: calc(var(--spacing-unit) * 4);     /* 32px */
--spacing-xxl: calc(var(--spacing-unit) * 6);    /* 48px */
```

#### 布局间距
```css
/* 容器内边距 */
.container-padding {
  padding: var(--spacing-lg);
}

/* 组件间距 */
.component-margin {
  margin-bottom: var(--spacing-md);
}

/* 表单元素间距 */
.form-item-spacing {
  margin-bottom: var(--spacing-sm);
}

/* 按钮间距 */
.button-group-spacing > * + * {
  margin-left: var(--spacing-sm);
}
```

### 1.4 圆角规范
```css
/* 圆角尺度 */
--border-radius-none: 0;
--border-radius-sm: 4px;
--border-radius-md: 8px;
--border-radius-lg: 12px;
--border-radius-xl: 16px;
--border-radius-full: 9999px;

/* 应用场景 */
.button { border-radius: var(--border-radius-md); }
.input { border-radius: var(--border-radius-sm); }
.card { border-radius: var(--border-radius-lg); }
.modal { border-radius: var(--border-radius-xl); }
.avatar { border-radius: var(--border-radius-full); }
```

### 1.5 阴影规范
```css
/* 阴影层级 */
--shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
--shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1),
             0 2px 4px -1px rgba(0, 0, 0, 0.06);
--shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1),
             0 4px 6px -2px rgba(0, 0, 0, 0.05);
--shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1),
             0 10px 10px -5px rgba(0, 0, 0, 0.04);

/* 应用场景 */
.card { box-shadow: var(--shadow-md); }
.modal { box-shadow: var(--shadow-xl); }
.button:hover { box-shadow: var(--shadow-sm); }
.input:focus { box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1); }
```

## 2. 组件规范

### 2.1 按钮组件

#### 按钮类型
```css
/* 主要按钮 */
.btn-primary {
  background-color: var(--color-primary);
  color: white;
  border: none;
  padding: var(--spacing-sm) var(--spacing-lg);
}

/* 次要按钮 */
.btn-secondary {
  background-color: var(--color-gray-200);
  color: var(--color-gray-700);
  border: 1px solid var(--color-gray-300);
  padding: var(--spacing-sm) var(--spacing-lg);
}

/* 文字按钮 */
.btn-text {
  background: transparent;
  color: var(--color-primary);
  border: none;
  padding: var(--spacing-xs) var(--spacing-md);
}

/* 图标按钮 */
.btn-icon {
  width: 40px;
  height: 40px;
  border-radius: var(--border-radius-full);
  display: flex;
  align-items: center;
  justify-content: center;
}
```

#### 按钮状态
```css
/* 悬停状态 */
.btn-primary:hover {
  background-color: var(--color-primary-dark);
}

/* 禁用状态 */
.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

/* 加载状态 */
.btn-loading {
  position: relative;
  color: transparent;
}

.btn-loading::after {
  content: "";
  position: absolute;
  width: 16px;
  height: 16px;
  border: 2px solid white;
  border-radius: 50%;
  border-top-color: transparent;
  animation: spin 1s linear infinite;
}
```

### 2.2 输入框组件

#### 文本输入
```css
.input-text {
  padding: var(--spacing-sm) var(--spacing-md);
  border: 1px solid var(--color-gray-300);
  border-radius: var(--border-radius-sm);
  font-size: var(--font-size-md);
}

.input-text:focus {
  outline: none;
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1);
}

/* 错误状态 */
.input-text.error {
  border-color: var(--color-error);
}

.input-text.error:focus {
  box-shadow: 0 0 0 3px rgba(231, 76, 60, 0.1);
}
```

#### 文件输入
```css
.input-file {
  position: relative;
  display: inline-block;
}

.input-file input[type="file"] {
  position: absolute;
  width: 100%;
  height: 100%;
  opacity: 0;
  cursor: pointer;
}

.file-drop-zone {
  border: 2px dashed var(--color-gray-300);
  border-radius: var(--border-radius-md);
  padding: var(--spacing-xl);
  text-align: center;
  transition: all 0.2s;
}

.file-drop-zone.dragover {
  border-color: var(--color-primary);
  background-color: rgba(52, 152, 219, 0.05);
}
```

### 2.3 进度条组件

#### 基础进度条
```css
.progress-container {
  width: 100%;
  height: 8px;
  background-color: var(--color-gray-200);
  border-radius: var(--border-radius-full);
  overflow: hidden;
}

.progress-bar {
  height: 100%;
  background-color: var(--color-primary);
  border-radius: var(--border-radius-full);
  transition: width 0.3s ease;
}

/* 状态变体 */
.progress-bar.success { background-color: var(--color-success); }
.progress-bar.warning { background-color: var(--color-warning); }
.progress-bar.error { background-color: var(--color-error); }
```

#### 带标签进度条
```css
.progress-with-label {
  display: flex;
  align-items: center;
  gap: var(--spacing-md);
}

.progress-label {
  min-width: 60px;
  text-align: right;
  font-size: var(--font-size-sm);
  color: var(--color-gray-600);
}

.progress-details {
  font-size: var(--font-size-xs);
  color: var(--color-gray-500);
  margin-top: var(--spacing-xxs);
}
```

### 2.4 表格组件

#### 基础表格
```css
.table {
  width: 100%;
  border-collapse: collapse;
}

.table th {
  background-color: var(--color-gray-100);
  padding: var(--spacing-sm) var(--spacing-md);
  text-align: left;
  font-weight: 600;
  color: var(--color-gray-700);
  border-bottom: 2px solid var(--color-gray-300);
}

.table td {
  padding: var(--spacing-sm) var(--spacing-md);
  border-bottom: 1px solid var(--color-gray-200);
}

.table tr:hover {
  background-color: var(--color-gray-50);
}
```

#### 可选中表格
```css
.table-selectable td:first-child {
  width: 40px;
  text-align: center;
}

.table-selectable input[type="checkbox"] {
  width: 16px;
  height: 16px;
}

.table-selectable tr.selected {
  background-color: rgba(52, 152, 219, 0.1);
}
```

### 2.5 卡片组件

#### 基础卡片
```css
.card {
  background-color: white;
  border-radius: var(--border-radius-lg);
  box-shadow: var(--shadow-md);
  padding: var(--spacing-lg);
}

.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--spacing-md);
  padding-bottom: var(--spacing-sm);
  border-bottom: 1px solid var(--color-gray-200);
}

.card-title {
  font-size: var(--font-size-h5);
  font-weight: 600;
  color: var(--color-gray-800);
  margin: 0;
}

.card-body {
  color: var(--color-gray-600);
}
```

### 2.6 模态框组件

#### 基础模态框
```css
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.modal-content {
  background-color: white;
  border-radius: var(--border-radius-xl);
  box-shadow: var(--shadow-xl);
  max-width: 600px;
  width: 90%;
  max-height: 90vh;
  overflow-y: auto;
}

.modal-header {
  padding: var(--spacing-lg);
  border-bottom: 1px solid var(--color-gray-200);
}

.modal-body {
  padding: var(--spacing-lg);
}

.modal-footer {
  padding: var(--spacing-lg);
  border-top: 1px solid var(--color-gray-200);
  display: flex;
  justify-content: flex-end;
  gap: var(--spacing-sm);
}
```

## 3. 布局规范

### 3.1 网格系统

#### 响应式网格
```css
.grid {
  display: grid;
  gap: var(--spacing-lg);
}

/* 列数响应 */
.grid-cols-1 { grid-template-columns: repeat(1, 1fr); }
.grid-cols-2 { grid-template-columns: repeat(2, 1fr); }
.grid-cols-3 { grid-template-columns: repeat(3, 1fr); }
.grid-cols-4 { grid-template-columns: repeat(4, 1fr); }

/* 响应式断点 */
@media (min-width: 640px) {
  .sm\:grid-cols-2 { grid-template-columns: repeat(2, 1fr); }
}

@media (min-width: 768px) {
  .md\:grid-cols-3 { grid-template-columns: repeat(3, 1fr); }
}

@media (min-width: 1024px) {
  .lg\:grid-cols-4 { grid-template-columns: repeat(4, 1fr); }
}
```

#### 间距响应
```css
.gap-0 { gap: 0; }
.gap-xs { gap: var(--spacing-xs); }
.gap-sm { gap: var(--spacing-sm); }
.gap-md { gap: var(--spacing-md); }
.gap-lg { gap: var(--spacing-lg); }
.gap-xl { gap: var(--spacing-xl); }
```

### 3.2 容器布局

#### 页面容器
```css
.container {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 var(--spacing-lg);
}

/* 响应式容器 */
@media (min-width: 640px) {
  .container { padding: 0 var(--spacing-xl); }
}

@media (min-width: 1024px) {
  .container { padding: 0 var(--spacing-xxl); }
}
```

#### 侧边栏布局
```css
.layout-sidebar {
  display: grid;
  grid-template-columns: 250px 1fr;
  gap: var(--spacing-lg);
  min-height: 100vh;
}

.sidebar {
  background-color: var(--color-gray-100);
  padding: var(--spacing-lg);
}

.main-content {
  padding: var(--spacing-lg);
}
```

### 3.3 响应式断点

#### 标准断点
```css
/* 移动端优先 */
:root {
  --breakpoint-sm: 640px;
  --breakpoint-md: 768px;
  --breakpoint-lg: 1024px;
  --breakpoint-xl: 1280px;
  --breakpoint-2xl: 1536px;
}

/* 媒体查询示例 */
@media (min-width: 640px) {
  /* 小屏幕样式 */
}

@media (min-width: 768px) {
  /* 中等屏幕样式 */
}

@media (min-width: 1024px) {
  /* 大屏幕样式 */
}

@media (min-width: 1280px) {
  /* 超大屏幕样式 */
}
```

## 4. 交互规范

### 4.1 动画规范

#### 持续时间
```css
--duration-fast: 150ms;
--duration-normal: 300ms;
--duration-slow: 500ms;
```

#### 缓动函数
```css
--ease-linear: linear;
--ease-in: cubic-bezier(0.4, 0, 1, 1);
--ease-out: cubic-bezier(0, 0, 0.2, 1);
--ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
```

#### 动画示例
```css
/* 淡入淡出 */
.fade-in {
  animation: fadeIn var(--duration-normal) var(--ease-in-out);
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

/* 滑动 */
.slide-up {
  animation: slideUp var(--duration-normal) var(--ease-out);
}

@keyframes slideUp {
  from { transform: translateY(20px); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}

/* 旋转 */
.spin {
  animation: spin 1s linear infinite;
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
```

### 4.2 状态反馈

#### 悬停状态
```css
.hover-lift {
  transition: transform var(--duration-fast) var(--ease-out);
}

.hover-lift:hover {
  transform: translateY(-2px);
}

.hover-glow {
  transition: box-shadow var(--duration-fast) var(--ease-out);
}

.hover-glow:hover {
  box-shadow: var(--shadow-lg);
}
```

#### 点击反馈
```css
.active-scale:active {
  transform: scale(0.95);
}

.ripple-effect {
  position: relative;
  overflow: hidden;
}

.ripple-effect::after {
  content: "";
  position: absolute;
  top: 50%;
  left: 50%;
  width: 5px;
  height: 5px;
  background: rgba(255, 255, 255, 0.5);
  opacity: 0;
  border-radius: 100%;
  transform: scale(1, 1) translate(-50%);
  transform-origin: 50% 50%;
}

.ripple-effect:focus:not(:active)::after {
  animation: ripple 1s ease-out;
}

@keyframes ripple {
  0% {
    transform: scale(0, 0);
    opacity: 0.5;
  }
  100% {
    transform: scale(40, 40);
    opacity: 0;
  }
}
```

## 5. 可访问性规范

### 5.1 键盘导航

#### 焦点样式
```css
:focus {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}

:focus:not(:focus-visible) {
  outline: none;
}

:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}
```

#### 焦点顺序
```css
/* 确保逻辑焦点顺序 */
.tab-index-0 { tab-index: 0; }
.tab-index--1 { tab-index: -1; }

/* 跳过导航链接 */
.skip-nav {
  position: absolute;
  top: -40px;
  left: 0;
  background: var(--color-primary);
  color: white;
  padding: var(--spacing-sm);
  z-index: 1001;
}

.skip-nav:focus {
  top: 0;
}
```

### 5.2 ARIA 属性

#### 按钮ARIA
```html
<button
  aria-label="开始解压"
  aria-busy="false"
  aria-disabled="false">
  开始
</button>
```

#### 进度条ARIA
```html
<div
  role="progressbar"
  aria-valuenow="75"
  aria-valuemin="0"
  aria-valuemax="100"
  aria-label="解压进度">
  <div class="progress-bar" style="width: 75%"></div>
</div>
```

#### 状态消息ARIA
```html
<div
  role="status"
  aria-live="polite"
  aria-atomic="true">
  解压完成，成功3个文件，失败1个文件
</div>
```

### 5.3 颜色对比度

#### 最小对比度要求
```css
/* 正常文本：4.5:1 */
.text-normal {
  color: var(--color-gray-800); /* 对比度: 15.8:1 on white */
  background-color: white;
}

/* 大文本：3:1 */
.text-large {
  font-size: var(--font-size-lg);
  color: var(--color-gray-700); /* 对比度: 10.5:1 on white */
  background-color: white;
}

/* 用户界面组件：3:1 */
.ui-component {
  color: var(--color-gray-900); /* 对比度: 21:1 on white */
  background-color: white;
  border: 2px solid var(--color-gray-600); /* 对比度: 7.4:1 */
}
```

## 6. 多语言规范

### 6.1 文本方向

#### RTL支持
```css
[dir="rtl"] .text-align-start {
  text-align: right;
}

[dir="rtl"] .margin-start {
  margin-right: var(--spacing-md);
  margin-left: 0;
}

[dir="rtl"] .float-start {
  float: right;
}
```

#### 双向文本
```css
.bidi-isolate {
  unicode-bidi: isolate;
  text-align: start;
}

.bidi-override {
  unicode-bidi: bidi-override;
  direction: rtl;
}
```

### 6.2 本地化考虑

#### 日期时间格式
```css
.date-format {
  /* 使用系统本地化格式 */
  font-variant-numeric: tabular-nums;
}

/* 不同语言的日期显示 */
[lang="en-US"] .date {
  /* MM/DD/YYYY */
}

[lang="zh-CN"] .date {
  /* YYYY-MM-DD */
}

[lang="ar-SA"] .date {
  /* DD/MM/YYYY (RTL) */
}
```

#### 数字格式
```css
.number-format {
  font-variant-numeric: tabular-nums;
}

/* 千位分隔符 */
[lang="en-US"] .number {
  /* 1,234.56 */
}

[lang="de-DE"] .number {
  /* 1.234,56 */
}

[lang="fr-FR"] .number {
  /* 1 234,56 */
}
```

## 7. 性能优化

### 7.1 图片优化

#### 响应式图片
```html
<img
  src="image-320w.jpg"
  srcset="image-320w.jpg 320w,
          image-480w.jpg 480w,
          image-800w.jpg 800w"
  sizes="(max-width: 320px) 280px,
         (max-width: 480px) 440px,
         800px"
  alt="描述文本"
  loading="lazy">
```

#### 图标系统
```css
.icon-system {
  /* 使用SVG精灵图或字体图标 */
  background-image: url('icons.svg');
  background-repeat: no-repeat;
}

/* 内联SVG */
.icon-inline {
  width: 24px;
  height: 24px;
  fill: currentColor;
}
```

### 7.2 字体优化

#### 字体加载策略
```css
@font-face {
  font-family: 'CustomFont';
  src: url('font.woff2') format('woff2');
  font-display: swap; /* 避免FOIT */
  font-weight: 400;
  font-style: normal;
}

/* 系统字体回退 */
.font-stack {
  font-family: 'CustomFont', -apple-system, BlinkMacSystemFont,
               'Segoe UI', Roboto, sans-serif;
}
```

## 8. 开发指南

### 8.1 命名约定

#### BEM命名法
```css
/* 块 */
.button { }

/* 元素 */
.button__icon { }
.button__text { }

/* 修饰符 */
.button--primary { }
.button--disabled { }
.button--loading { }
```

#### 工具类命名
```css
/* 间距 */
.mt-1 { margin-top: var(--spacing-xs); }
.mt-2 { margin-top: var(--spacing-sm); }
.mt-3 { margin-top: var(--spacing-md); }

/* 颜色 */
.text-primary { color: var(--color-primary); }
.bg-primary { background-color: var(--color-primary); }

/* 显示 */
.d-block { display: block; }
.d-flex { display: flex; }
.d-none { display: none; }
```

### 8.2 代码组织

#### CSS文件结构
```
styles/
├── base/           # 基础样式
│   ├── reset.css
│   ├── typography.css
│   └── variables.css
├── components/     # 组件样式
│   ├── button.css
│   ├── input.css
│   ├── table.css
│   └── modal.css
├── layouts/        # 布局样式
│   ├── grid.css
│   ├── sidebar.css
│   └── container.css
├── utilities/      # 工具类
│   ├── spacing.css
│   ├── display.css
│   └── text.css
└── themes/         # 主题样式
    ├── light.css
    ├── dark.css
    └── high-contrast.css
```

#### 组件文件结构
```
components/
├── Button/
│   ├── Button.jsx
│   ├── Button.css
│   ├── Button.test.js
│   └── index.js
├── ProgressBar/
│   ├── ProgressBar.jsx
│   ├── ProgressBar.css
│   ├── ProgressBar.test.js
│   └── index.js
└── FileList/
    ├── FileList.jsx
    ├── FileList.css
    ├── FileList.test.js
    └── index.js
```

---

**规范版本**: v1.0
**最后更新**: 2026-03-08
**适用平台**: Web, Desktop (Electron), CLI