# 核心压缩与解压流程稳定化计划

> 建立日期：2026-07-28
> 影响版本：v1.0.14
> 目标发布：v1.0.15
> 当前状态：阶段 1–4 已实现，常用格式真实 Windows 验收通过并进入发布
> 目标：在继续性能拆分或框架升级前，先让压缩、解压、取消、进度和任务管理形成可重复验证的完整闭环。

## 1. 用户反馈与审计结论

### 1.1 同一压缩目标被重复执行

用户日志中同一个 `F:\下载\LongAssistant-Setup.7z` 出现两次“开始压缩到”，随后一个任务完成，
另一个任务因目标文件在运行期间出现而失败：

```text
02:19:37 开始压缩到: F:\下载\LongAssistant-Setup.7z
02:20:05 开始压缩到: F:\下载\LongAssistant-Setup.7z
02:20:10 压缩完成
02:20:38 压缩失败: Output appeared while compression was running
```

后端的临时文件和最终提交保护避免了覆盖已有文件，但当前没有“规范化最终输出路径占用”机制；
前端也只检查单次批次内部的重复输出，没有检查正在运行的任务。页面重新挂载、自动启动和手动重试
都可能让同一目标再次提交。

### 1.2 7Z 长时间停留在 0%

7Z 解压在显示格式后，会先通过外部 7-Zip 完整测试归档以判断加密状态。该阶段没有进度和阶段提示。
随后内置 7Z 压缩与解压按“条目完成”更新进度，单个大文件在完整处理结束前会一直显示 0%。
7Z 写入单个条目期间也不能及时检查取消令牌。

### 1.3 压缩任务出现在解压中心

压缩和解压共用全局任务列表，但解压中心和任务表格没有按任务类型过滤。解压中心的“开始队列”
还会选择全部 `pending` 任务，存在把待压缩任务当作解压任务处理的风险。

### 1.4 压缩中心没有完整任务生命周期

压缩中心当前展示的是待配置源文件；点击整行用于多选打组，齿轮按钮才展开设置。开始压缩后，
真正的任务进入全局任务 Store，但压缩中心不展示状态、进度、日志、取消、失败或完成记录。
这些记录反而会出现在解压中心。

## 2. 修复原则

1. 任务类型必须在查询、选择、启动、统计、清理和展示的每一层隔离。
2. 同一个规范化最终输出路径在任意时刻只能有一个活动压缩任务。
3. “请求取消”不等于“取消完成”；后端退出并清理临时输出后，任务才能进入 `cancelled`。
4. 每个超过数秒的阶段都必须有可见状态；可以暂时无法精确计量，但不能表现为无响应。
5. 7Z 单个大文件必须提供字节级进度，并在块边界响应取消。
6. 待配置条目与已提交任务分离；提交成功后不允许无提示地重复使用相同输出。
7. 修复必须由自动化回归和真实 Windows 文件测试共同证明。

## 3. 分步实施

### 阶段 1：任务隔离与执行幂等

- 为任务 Store 增加按 `compression` / `decompression` 查询和清理能力。
- 解压中心只显示、选择、启动和统计解压任务。
- 压缩中心只操作压缩任务。
- 后端增加最终输出路径占用表，拒绝并发写入同一目标。
- 前端提交前检查已有活动压缩任务的输出路径，并给出可理解提示。
- 成功、失败和取消后必须正确释放路径占用。

验收：

- 压缩任务不会出现在解压中心。
- 解压中心“开始队列”不会调用任何压缩任务。
- 两个任务并发请求同一输出时，第二个在写入前立即失败，且第一个产物不受影响。
- 页面切换、自动启动和重复点击不能绕过后端保护。

### 阶段 2：7Z 阶段、进度与取消

- 将加密状态判断从无提示的完整归档测试中拆出，至少显示“正在检测归档”阶段。
- 评估通过 7Z 元数据判断加密状态，避免无密码归档在正式解压前被完整测试一次。
- 7Z 压缩和解压按已处理字节上报进度；多文件同时保留当前文件名。
- 单个大文件的压缩、解压和取消必须在可接受时间内产生反馈。
- 取消后清理临时压缩文件和解压暂存目录。

验收：

- 1 GiB 单文件 7Z 压缩和解压期间，进度持续变化而不是长期停留在 0%。
- 点击取消后 2 秒内进入“正在取消”，后端结束后进入“已取消”。
- 取消不会留下最终成品、随机临时文件或解压暂存目录。

### 阶段 3：压缩中心任务化重构

- 行首提供独立复选区域，只用于多选打组。
- 点击任务行主体展开/收起，不再隐式改变打组选择。
- 展开区域采用双列布局：
  - 左侧：格式、级别、密码、输出目录、文件名、分卷和路径结构；
  - 右侧：状态、阶段、进度、当前文件、速度、日志和取消操作。
- 待配置条目提交后转换为压缩任务。
- 压缩中心提供“清理已完成”、单项删除、取消和失败重试。
- 解压任务卡与压缩任务卡复用同一视觉骨架，但只显示各自适用的配置。

验收：

- 压缩任务从等待、压缩中、收尾、完成/失败/取消均有明确状态变化。
- 压缩中心可以查看完整日志并一键清理自己的完成记录。
- 清理压缩记录不会删除解压记录，反之亦然。

### 阶段 4：完整回归与真实验收

自动化测试：

- 前端：任务类型过滤、选择、清理、页面切换、重复启动、任务卡展开和状态转换。
- Rust：输出占用、释放、并发竞争、失败回滚、7Z 进度和取消清理。
- 桌面 E2E：拖入文件、开始压缩、查看进度、取消、重试、完成后解压并逐字节比较。

真实 Windows 测试矩阵：

| 场景 | 数据 | 必须验证 |
| --- | --- | --- |
| 7Z 单文件 | 1 GiB | 连续进度、取消响应、重新执行 |
| 7Z 多文件 | 10,000 个小文件 | 当前文件、总体进度、完成清理 |
| ZIP 单文件 | 1 GiB | 连续进度、取消、哈希一致 |
| 同输出竞争 | 两个任务写同一目标 | 第二个立即拒绝、第一份不损坏 |
| 页面切换 | 压缩中切换到解压中心再返回 | 不重复执行、任务不串台 |
| 完整闭环 | 源文件 → 7Z → 解压目录 | 目录结构、大小和 SHA-256 一致 |

## 4. 2026-07-28 实施与测试记录

已完成：

- 压缩/解压任务在查询、启动、展示、统计和清理层完全按类型隔离。
- 前端提交前拒绝与活动任务相同的输出；后端按规范化绝对路径原子占用输出。
- 已校验的压缩草稿在提交后立即转为任务，不再保留为可重复点击的草稿。
- 7Z 加密判断改为读取归档元数据，不再先执行一次无进度的完整归档测试。
- 原生 7Z 压缩与解压按已处理字节持续上报进度，并在数据块边界检查取消。
- 取消状态拆分为 `cancelling` 和 `cancelled`；后端退出并释放任务注册后，前端才显示已取消。
- 压缩中心已具备独立任务列表、展开详情、配置摘要、当前阶段、进度、日志、取消进行中和清除已结束。
- 待压缩条目整行用于展开详情，只有行首复选框用于打组。

首轮测试证据：

| 测试层 | 结果 |
| --- | --- |
| TypeScript 类型检查 | 通过 |
| 前端单元/组件测试 | 24 个文件、136 项全部通过 |
| Rust 7Z 服务回归 | 17 项全部通过 |
| Rust 输出占用与取消回归 | 4 项全部通过 |
| Rust 常用格式往返与通用引擎冒烟 | 19 + 1 项全部通过 |
| `cargo clippy --lib -- -D warnings` | 通过 |
| 正式前端构建 | 通过 |
| 真实 Windows Tauri E2E | 通过 |

真实桌面 E2E 使用正式 Tauri 命令和原生归档引擎，不使用压缩 mock：

- 24 MiB 随机二进制文件完成 `源文件 → 7Z → 解压目录`，解压结果逐字节一致。
- 7Z 压缩和解压均捕获到大于 0 且小于 100 的中间字节进度。
- 96 MiB 随机二进制文件在真实 7Z 压缩中成功取消，未留下最终归档。
- 真实桌面往返矩阵覆盖 ZIP、7Z、TAR、TAR.GZ、TAR.BZ2、TAR.XZ、GZ、BZ2、XZ、
  Zstandard、TAR.ZST、LZMA、加密 ZIP 和加密 7Z；每项均通过应用真实命令创建、解压并逐字节比对。
- 矩阵首次运行发现随附 7-Zip 无法执行 `a -tlzma`；LZMA 创建已改为原生 LZMA-Alone
  编码器，修复后从头重跑矩阵并通过。
- ZIP 快速打包/解压、确定性取消清理、活动任务退出确认、更新阻断、托盘隐藏和第二实例恢复继续通过。

尚未作为本轮发布门禁完成的压力矩阵：

- 1 GiB 单文件长时间运行。
- 10,000 个小文件的当前文件与总体进度。
- 两个真实桌面命令同时竞争同一最终输出（后端原子占用已有 Rust 回归覆盖）。
- RAR、CAB、ISO、WIM、DMG、VHD/VHDX、MSI 等通用引擎格式的真实外部样本解压矩阵。

这三项属于扩大规模的压力验收，不影响本次用户反馈所对应的 24/96 MiB 实际故障链路已经跑通；正式升版前仍按下方门禁决定是否补跑。

## 5. 发布门禁

在以下条件全部满足前，不提升新版本、不创建正式 Release：

- 阶段 1–3 的 P0/P1 自动化测试通过。
- 真实 Windows 单大文件和多小文件矩阵通过。
- 同输出竞争、取消清理和页面切换回归通过。
- 压缩中心与解压中心各自只展示和操作自己的任务。
- v1.0.14 发布验收 Issue 中未完成的安装、升级、右键菜单和卸载项目另行保留，不用重复任务掩盖。

## 6. 本轮明确不混入

- 不在本轮迁移 Tauri 2、Vite/Vitest 或 Pinia 主版本。
- 不启用实验性 `ParallelExtractor`。
- 不处理需要可信代码签名证书的 Windows 11 顶层右键菜单发布。
- 不在核心正确性修复中进行全仓 Rust 格式化或无关视觉改版。
