# Long解压开发审计与后续计划

> 审计日期：2026-07-28
> 审计基线：`master` / `b9917fddac90d004ecc57d9024372b71fd0a4df5`
> 当前正式版本：`v1.0.16`
> 本次范围：只更新开发与发布文档，不提升版本、不重打包、不重建 Release。

## 1. 审计结论

Long解压的核心压缩、解压和 Windows 桌面生命周期已经具备可继续迭代的稳定基线。当前不应继续堆叠新入口，而应先完成发布治理闭环，再拆分高复杂度模块并补足真实安装环境的持续验收。

当前状态可以概括为：

- `v1.0.16` 已正式发布，PR、`master` CI 和 Release 工作流均成功。
- 压缩中心已经具备与解压中心一致的任务行、抽屉动效、左侧配置和右侧执行详情。
- 压缩、解压任务已按类型隔离；同输出并发、取消竞态、任务清理和真实进度已有回归保护。
- 密码保险箱首屏加载、密码显示/复制、字段命名和列宽已经完成可用性收口。
- Windows 11 顶层右键菜单仍受可信代码签名证书限制；当前明确暂缓，不作为近期开发项。
- 当前最大的产品风险不是缺少功能，而是正式版本、候选构建和人工验收之间的身份管理仍不够严格。

## 2. 已核验的工程状态

### 2.1 GitHub 与发布

- `master` 最新提交：`b9917fd`，对应 `v1.0.16`。
- `v1.0.16` Release 工作流成功，公开资产包含：
  - Windows NSIS 安装程序；
  - updater `.nsis.zip`；
  - updater `.sig`；
  - `latest.json`。
- `master` 已开启分支保护：
  - 必须通过 PR；
  - 要求分支为最新状态；
  - 要求解决审查对话；
  - 禁止强推和删除。
- 当前唯一未关闭 Issue 是 [#15](https://github.com/Longyuyeee/long_Decompress/issues/15)，其中仍有覆盖安装、应用内升级、传统右键菜单和卸载的人工验收项未完成。

### 2.2 自动化与依赖

本次本机审计结果：

- `npm.cmd run type-check`：通过。
- `npm.cmd run test:unit`：24 个测试文件、141 项测试通过。
- `npm.cmd run test:unit:coverage`：27 个测试文件、162 项测试通过。
- 当前覆盖率：
  - Statements/Lines：70.38%；
  - Branches：74.58%；
  - Functions：59.00%。
- `npm audit --omit=dev --registry=https://registry.npmjs.org`：生产依赖 0 个已知漏洞。
- 最新 PR CI 的 Frontend、Rust/Shell Extension、Browser E2E、Windows desktop E2E build 和 Windows installer 均通过。

### 2.3 结构复杂度

当前需要优先控制的代码热点：

| 文件 | 规模 | 审计判断 |
| --- | ---: | --- |
| `src-tauri/src/services/compression_service.rs` | 4,255 行 | 同时承担格式路由、归档实现、安全限制、进度、取消和事务提交，修改风险最高 |
| `src/views/CompressionView.vue` | 1,162 行 | 任务编排、状态转换、配置和大量视觉结构集中在同一组件 |
| `src/views/DecompressView.vue` | 805 行 | 仍可维护，但后续应与压缩中心共享任务详情模型和展示组件 |
| `src-tauri/src/services/aes_stream_v2.rs` | 783 行 | 独立边界较清晰，应保持格式兼容测试先行 |

现阶段不建议直接大规模重写。拆分必须以现有行为测试为保护，每次只移动一个明确职责。

## 3. 已确认的发布治理问题

### 3.1 同版本不同构建

开发机曾安装较早构建的 `1.0.16`，随后最终代码仍以 `v1.0.16` 发布。Tauri updater 只比较语义版本，不比较二进制内容，因此较早的 `1.0.16` 无法自动更新到较新的 `1.0.16`。

本次按要求不提升版本，也不重建 Release。后续必须遵守：

1. 正式版本号一旦被本机安装、交付测试或公开分发，不得再用于不同提交。
2. 开发构建使用 `-dev`，候选构建使用 `-rc.N`，只有最终候选通过后才能使用正式版本号。
3. 每个正式版本号只能对应一个 Git 提交、一个标签和一组公开资产。
4. 功能或二进制内容再次变化时，下一个正式 Release 必须使用高于 `1.0.16` 的版本号。

### 3.2 更新检查可观测性不足

自动更新检查当前存在 24 小时间隔，并在发起检查前写入最后检查时间。网络失败也可能导致下一次自动检查延后；界面也缺少当前版本、最后检查时间和最后结果的集中展示。

后续应把“最后尝试时间”和“最后成功时间”分开记录，并在设置中心显示：

- 当前安装版本；
- 线上最新版本；
- 最后成功检查时间；
- 最近一次失败原因；
- 手动检查按钮的实时状态。

### 3.3 人工验收未闭环

现有 Issue #15 仍停留在 `v1.0.14`，没有为 `v1.0.15` 和 `v1.0.16` 建立完整记录。自动化成功不能替代公开安装包在真实已安装环境中的升级、右键菜单和卸载验收。

## 4. 后续开发计划

### P0：发布身份与升级闭环

目标：确保以后每个版本都能被旧版本可靠识别、下载、验证和安装。

1. 在发布检查中增加版本身份门禁：
   - 四处版本号完全一致；
   - 标签版本等于应用版本；
   - 正式版本号不得对应另一个提交或已分发构建；
   - Release 资产和 `latest.json` 必须指向同一版本。
2. 在候选版本流程中引入 `-rc.N`，正式版本号只在最终发布提交使用。
3. 为下一正式版本建立发布验收 Issue，并实际验证：
   - 从公开的 `v1.0.16` 自动更新；
   - 设置和密码保险箱数据保留；
   - 更新过程中活动任务阻断；
   - 更新完成后的版本和二进制来源一致。
4. 完善更新页的版本与检查状态可观测性。

完成标准：任意公开安装版本都能明确判断是否存在更新；同版本不同构建无法进入正式交付流程。

### P1：公开安装包真实回归

目标：把“代码通过”推进到“用户实际安装后可用”。

1. 使用 GitHub Release 公开安装包，而不是本地安装包，执行：
   - ZIP、7Z 压缩与解压；
   - 密码 ZIP、密码 7Z 和 RAR 解压；
   - 多文件一键打包和一键解压；
   - 取消、冲突、磁盘不足和失败回滚；
   - 托盘、传统右键菜单、覆盖安装和卸载。
2. 保存安装包哈希、Windows 版本、测试结果和失败证据。
3. 有固定交互式 Windows 主机后，将真实 GUI 套件接入 self-hosted runner。

完成标准：发布记录可以从 Release 回链到自动化结果和人工安装验收证据。

### P1：组件边界与交互回归

目标：降低压缩中心继续迭代时的视觉和状态回归概率。

1. 从 `CompressionView.vue` 提取稳定边界：
   - 任务摘要行；
   - 配置详情；
   - 阶段/进度/日志面板；
   - 批量操作栏。
2. 压缩与解压中心共享任务执行详情的数据模型和基础展示组件。
3. 保持现有视觉、键盘操作、展开动效和测试选择器不变。
4. 补充窄窗口、长文件名、大任务量和日志持续增长场景。

完成标准：组件拆分不改变用户行为，现有 162 项覆盖率测试不减少，压缩中心专项测试保持通过。

### P2：后端归档服务拆分

目标：降低 `compression_service.rs` 的单文件复杂度。

建议顺序：

1. 提取格式能力判断和路由。
2. 提取输出占用、暂存和事务提交。
3. 分离 ZIP、7Z、TAR/单文件流实现。
4. 分离密码验证、AES 容器和资源限制策略。
5. 最后评估 `ParallelExtractor`：完整对齐生产语义后接入，否则删除实验入口。

完成标准：每次拆分都只改变代码结构，不改变归档格式、错误语义、取消行为和输出提交规则。

### P2：性能趋势与依赖现代化

1. 在固定 Windows 环境累计至少 10 次大文件、小文件和 AES 基准样本，再设置回归阈值。
2. CI 与 Release 的 Node/Action 主版本保持一致，减少两个工作流环境漂移。
3. 低风险依赖更新与 Tauri 2、Vite/Vitest 主版本迁移分开处理。
4. 每次主版本迁移都重新验证安装器、updater、托盘和右键菜单。

### 暂缓：Windows 11 顶层右键菜单

当前没有可信 Windows 代码签名证书。继续维护传统“显示更多选项”菜单和测试身份包，不把顶层菜单列入近期交付承诺。

## 5. 推荐执行顺序

1. 发布身份门禁和更新状态可观测性。
2. 下一正式版本的公开安装包升级验收闭环。
3. 压缩中心组件拆分与大任务量视觉回归。
4. `compression_service.rs` 按职责渐进拆分。
5. 固定环境性能趋势。
6. 独立处理框架主版本迁移。
7. 代码签名证书具备后再恢复 Windows 11 顶层菜单交付。

## 6. 本轮不执行

- 不提升 `1.0.16` 版本号。
- 不修改或覆盖现有 `v1.0.16` 标签和 Release。
- 不把相同版本号下的不同构建伪装成可自动更新版本。
- 不在文档审计 PR 中混入功能、依赖或架构代码修改。
