# 🐛 Bug 修复任务清单

根据用户反馈，发现以下问题需要修复：

## 📋 已识别的 Bug

### 1. ❌ **严重**: 无密码文件提示密码错误
**问题描述**: 解压一个没有密码的文件时，错误提示密码错误

**根本原因**: 
- 在 `DecompressView.vue:258-270` 中，即使文件不需要密码，也会自动从保险箱获取候选密码
- 如果保险箱有候选密码，会先尝试使用，导致无密码文件被当成加密文件处理
- 错误检测逻辑 (`errorMsg.includes('password')`) 过于宽泛，任何包含"password"的错误都被认为是密码错误

**修复方案**:
1. 只在后端明确返回 `PasswordRequired` 错误时才尝试密码
2. 修改错误检测逻辑，只处理真正的密码错误类型
3. 不要预先给无密码文件添加密码

**位置**: `src/views/DecompressView.vue:258-306`

---

### 2. ⚠️ **中等**: 完整性校验界面内容溢出
**问题描述**: 完整性校验界面内容溢出屏幕

**根本原因**: 
- 结果列表没有固定高度和滚动容器
- 长文件路径没有换行处理
- 校验和字符串过长导致水平溢出

**修复方案**:
1. 给结果区域添加 `max-h` 和 `overflow-y-auto`
2. 文件名和路径使用 `truncate` 或 `break-all`
3. 校验和使用 `break-all` 允许换行

**位置**: `src/views/FileIntegrityView.vue:272-313`

---

### 3. 🔴 **严重**: 任务消失问题 - 需要重构生命周期
**问题描述**: 点击开始解压/压缩后，任务直接消失，没有状态变化过程

**期望行为**:
- 未开始 → 正在解压/压缩（带进度动画）→ 已完成/错误
- 任务条目不消失，停留在列表中
- 右上角添加"清理已完成"按钮

**根本原因**: 
- 当前逻辑：任务开始后立即从 pending 变为 running/extracting，界面上可能过滤了非 pending 状态
- 缺少"清理"功能，完成的任务没有手动清理机制

**修复方案**:
1. 修改 `taskStore` 不自动删除完成的任务
2. 任务列表显示所有状态：pending / running / completed / failed
3. 添加状态图标和颜色编码
4. 右上角添加"清理已完成"按钮，手动清理 completed/failed 任务

**位置**: 
- `src/stores/task.ts` - 任务状态管理
- `src/views/DecompressView.vue` - 解压界面
- `src/views/CompressionView.vue` - 压缩界面
- `src/components/tasks/AeroTable.vue` - 任务表格组件

---

### 4. 🔴 **严重**: 压缩中心布局溢出
**问题描述**: 
- 目标格式那一行太长，界面超出很多
- 全局设置摊开在桌面上，占用大量空间
- 很多按钮功能不明确

**修复方案**:
1. **全局设置改为弹窗**:
   - 在"添加文件/文件夹"右边加一个"全局设置"按钮
   - 点击后在右下角弹出设置面板（类似 modal）
   - 弹窗内放置全局设置选项

2. **简化主界面**:
   - 移除摊开的全局设置区域
   - 目标路径放在外面（常用选项）
   - 管理配置组放到弹窗中

3. **格式选择优化**:
   - 将格式选择做成下拉菜单或网格布局
   - 不要平铺所有格式（太长）

**位置**: 
- `src/views/CompressionView.vue:332-385` - 全局设置区域
- `src/components/compression/CompressionSettingsPanel.vue` - 设置面板组件

---

### 5. ⚠️ **中等**: 密码生成器按钮无效
**问题描述**: 解压密码右边的骰子按钮点击无反应

**根本原因**: 
- 未找到骰子图标的使用位置（可能是 `pi-refresh` 或类似图标）
- 可能缺少点击事件绑定
- 或者功能未实现

**修复方案**:
1. 查找密码输入框旁边的图标按钮
2. 添加 `@click` 事件绑定
3. 实现随机密码生成功能（可以打开 PasswordGeneratorDialog）

**位置**: 需要进一步定位

---

## 📊 优先级排序

| 优先级 | Bug | 影响范围 | 修复难度 |
|--------|-----|----------|----------|
| **P0** | #3 任务消失问题 | 核心功能 | 高（需重构） |
| **P0** | #4 压缩中心布局溢出 | 用户体验 | 中（UI重构） |
| **P1** | #1 无密码文件密码错误 | 功能逻辑 | 低（条件判断） |
| **P2** | #2 完整性校验溢出 | 界面问题 | 低（CSS修复） |
| **P3** | #5 密码生成器按钮 | 辅助功能 | 低（事件绑定） |

---

## 🛠️ 修复计划

### 阶段 1: 紧急修复（P0）
1. 重构任务生命周期（#3）
2. 重构压缩中心布局（#4）

### 阶段 2: 功能修复（P1-P2）
3. 修复密码错误提示（#1）
4. 修复完整性校验溢出（#2）

### 阶段 3: 体验优化（P3）
5. 修复密码生成器按钮（#5）

---

## ✅ 下一步行动

立即开始修复 P0 问题：
1. Task #3 - 重构任务生命周期
2. Task #4 - 重构压缩中心布局

