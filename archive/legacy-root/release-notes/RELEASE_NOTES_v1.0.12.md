# Long解压 v1.0.12

本版本集中优化品牌图标、设置中心状态辨识度和密码保险箱的首次使用体验。

## 品牌与视觉

- 放大 L 形拉链图标的有效内容区域，减少外围透明留白。
- 重新生成 PNG、ICO、ICNS 和 Windows Appx 全尺寸图标资源。
- 移除应用左上角品牌图标额外的描边、渐变底框和阴影。
- 强调色在亮色、暗色、赛博、暮色、护眼和跟随系统主题下均可生效。
- 强调色选中状态增加颜色名称、勾选、描边和光环。
- 设置开关使用中性灰关闭态与强调色开启态，状态区别更加清晰。

## 密码保险箱

- 首次进入时使用稳定骨架屏，避免空数据与真实数据之间闪烁。
- 密码名称、密码正文和备注三列统一为相同宽度。
- 每条密码新增独立显示/隐藏按钮，并保留复制按钮。
- “创建新凭证”改为“创建新密码”，“凭证名称”改为“密码名称”，“访问密码”改为“密码正文”。
- 移除手动锁定、主密码输入和闲置自动锁定功能；本机密码数据仍使用每个安装实例自动生成的密钥加密，不会上传网络。
- 自动初始化失败时提供明确的重新加载入口，不再要求用户输入未设置过的主密码。

## 验证

- TypeScript 类型检查通过。
- 前端完整单元测试通过。
- Tauri/Rust 测试与 Windows 安装包构建通过。
- Windows 公开安装包仍不包含需要可信代码签名证书的 Windows 11 新式顶层菜单身份包，传统右键菜单继续可用。
