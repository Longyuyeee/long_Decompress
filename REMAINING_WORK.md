# 开发收口清单

> 最后更新：2026-07-30 | 当前正式版本：v1.0.19

完整审计、风险分级和阶段验收标准见 [DEVELOPMENT_ROADMAP.md](DEVELOPMENT_ROADMAP.md)。

## 当前结论

v1.0.14 暴露的同输出重复压缩、7Z 长时间停留在 0%、任务串台和压缩中心生命周期缺失已经完成
阶段 1–4 修复；v1.0.16 完成压缩中心单条目生命周期，v1.0.17 进一步对齐任务行并补齐密码保险箱持久化、生命周期和长期统计。24 MiB 随机文件的真实 7Z 压缩/解压逐字节一致，
96 MiB 真实 7Z 取消未留下最终归档；常用格式桌面矩阵包含加密 ZIP/7Z 和 LZMA。
自动化和真实 Windows 证据见
[核心压缩与解压流程稳定化计划](CORE_WORKFLOW_STABILIZATION.md)。

## 当前质量门

1. 前端覆盖率套件 30 个测试文件、182 项通过，当前为 75.22% 行/语句、78.03% 分支、63.68% 函数；门槛同步提高到 75% 行/语句、75% 分支和 60% 函数。
2. 交互式 Windows 严格全格式桌面 E2E 已覆盖 25 种可创建格式，以及 QCOW2、VDI、VMDK、VHD/VHDX、FAT16、NTFS、APFS、SquashFS、MSI/MSM/MSP、UEFI 和固定上游只读样本；同时覆盖取消、密码保险箱、托盘和应用生命周期。
3. 使用管理员 PowerShell 执行 `npm run test:context-menu-package`，验证测试身份包安装、COM 激活、资源管理器菜单和清理回滚。
4. `master` 已要求通过 PR 合入、四项 CI 成功、解决对话，并禁止强制推送和删除；管理员仅保留紧急绕过能力。
5. 每次正式发布按 [RELEASE_CHECKLIST.md](RELEASE_CHECKLIST.md) 创建发布验收记录，并从 Release 回链。

## 尚未对齐的外部依赖

1. 当前没有可信 Windows 代码签名证书，证书采购与 Actions Secrets 配置暂缓。
2. 在获得可信证书前不发布 Windows 11 新式右键菜单身份包；公开版本只保证“显示更多选项”中的传统菜单。
3. 无商业代码签名时 SmartScreen 仍可能警告；Release 必须明确提示用户核对下载来源。

## 后续工程工作

1. 在固定 Windows 环境积累 AES v2 性能趋势；`AESENC01/TARAES01` 保持受限的只读兼容。
2. 真实 Tauri Windows 桌面 E2E 已覆盖第二实例转发、常用格式压缩/解压逐字节闭环、长任务取消及残留清理、活动任务退出确认、更新阻断和托盘隐藏/恢复。接入交互式 self-hosted Windows runner 属于后续基础设施增强。
3. `CompressionView.vue` 已提取工具栏、状态进度和执行日志组件并直接回归；压缩配置组补齐前后端命名归一化、复杂密码策略和异常路径测试。
4. 将 100MiB/1GiB ZIP、小文件和后续加密基准放入固定 Windows 环境做趋势采样，再制定性能回归阈值。
5. 评估实验性 `ParallelExtractor`：必须先对齐密码、冲突、时间戳、回滚和路径安全，否则不进入生产路径。
6. `compression_service.rs` 的格式识别、能力矩阵、执行路由、暂存、资源限制和事务提交已拆分到独立模块；
   下一步按 ZIP/7Z/TAR/单文件流拆分原生解压引擎，继续以真实归档矩阵和失败回滚测试为行为保护。
7. 分批清理 Rust 历史格式化欠账；不与可靠性修复混合。
8. Tauri 2、Vite/Vitest、Pinia 等主版本升级在独立迁移分支评估。

## 下一步执行顺序

1. v1.0.19 已发布；公开 v1.0.18 → v1.0.19 的签名更新、自动重启、数据保持、安装目录、
   单一 Shell DLL、传统右键菜单和无 MSIX 残留共 18 项全部通过。
2. 继续寻找可再分发的非空 HFSX 样本或可靠写入工具；真实载荷闭环前不标记完整支持。
3. 已完成压缩中心稳定组件拆分，整体行覆盖率达到 75.22%，新的防倒退门槛已生效。
4. 阶段 D 首个边界已完成：格式路由/能力判断已提取，所有声明别名和 `.tpz` TAR 包装路由已有防倒退测试；
   `compression_service.rs` 从 4,281 行降至 3,961 行。
5. 阶段 D 第二个边界已完成：暂存、资源限制、冲突决策和事务回滚已提取，核心文件进一步降至 3,613 行；
   提前返回暂存残留、空目录资源规避、Windows reparse point 和静默回滚失败均已加固。
6. 下一步按 ZIP/7Z/TAR/单文件流拆分原生解压引擎，并在固定环境积累性能趋势。

## v1.0.13 已完成

- 文件筛选通配符改为每个任务只编译一次；原生 7Z、密码 ZIP 压缩与解压统一使用 256KiB 分块复制，并补齐大条目内部取消响应。
- 完成首批归档性能优化：复用 256KiB 复制缓冲、按 4MiB 节流原生解压进度、避免缓冲池反复收缩、降低通用引擎全目录扫描频率，并合并暂存布局整理与资源计数；新增 100MiB/1GiB 真实文件 CRC32 与峰值工作集回归。
- 密码保险箱密钥目录创建失败改为返回可诊断错误，不再直接 panic。
- 分卷归档文件名无法转换时返回错误，不再通过 `unwrap` 终止进程。
- 新增持续集成质量门：类型检查、覆盖率、生产构建、Rust 全目标测试、Clippy、Shell Extension、Chromium E2E、Windows 桌面 E2E 构建和无签名 NSIS 构建。
- 删除 4 个长期通过 `cfg(any())` 隐藏、且依赖已移除 API 的腐化测试文件；对应能力由当前数据库/配置集成测试、密码流程测试和服务单测覆盖。
- 本机无 updater、无代码签名证书的 NSIS 构建成功，确认普通 CI 不依赖更新私钥。
- 更新弹窗和全局任务进度条新增关键交互回归；更新安装会在活动任务结束前保持禁用，安装中弹窗不可被关闭。
- 通用模态框修复“首次即为可见状态时不自动聚焦”的问题，并新增 Tab 循环和 Escape 回归测试。
- 移除 encrypted vault 前端 Store 中从未被界面调用、且始终抛错的“归档条目”假入口；不再向调用方暴露不可用能力。
- 修复 `context-quick-pack` 因分发条件不可达而错误进入解压中心的问题，一键打包现在进入真实快速 ZIP 流程。
- 应用入口新增启动、快捷键、右键任务排空、后台运行和取消任务后退出的编排回归；同时避免键盘事件目标不是 DOM 元素时调用 `matches` 崩溃。
- 文件完整性页新增混合成功/失败计算、复制、导出和校验失败回归，页面行覆盖率达到 92.83%。
- 覆盖率防倒退门槛已进一步提高到 75% 行/语句、75% 分支和 60% 函数。

## 已稳定覆盖的核心流程

- 动态归档引擎能力检测和真实 ZIP/7Z/TAR/WIM/扩展格式样本矩阵。
- 未加密归档不会进入密码字典攻击，也不会报告虚假的“密码破解成功”。
- 加密 ZIP、7Z、RAR 解压和密码候选验证；ZIP/7Z/RAR 与 AES 封装的加密创建能力按运行时能力开放。
- 事务式解压暂存、冲突策略、失败回滚、路径穿越和资源上限防护。
- 标准分卷 ZIP、分卷识别、缺失输入保护和取消清理。
- 托盘运行、关闭最小化、一键解压/打包任务分发和传统资源管理器右键菜单。
- 签名应用内更新清单、更新包下载地址和 Release 资产生成。
