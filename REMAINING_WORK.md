# 剩余工作清单

> 最后更新：2026-07-18 | 当前发布版本：v1.0.3

## 发布前验收

1. 提交并推送当前修复，确认根目录 GitHub Actions 的 Windows CI、NSIS 安装包和 Release 工作流真实通过。
2. 在已安装的 Windows 应用中执行人工冒烟：ZIP/7Z 压缩、加密 ZIP、RAR 解压、密码保险箱、右键菜单与卸载。
3. 为自动更新配置签名公钥；正式分发前评估 Windows 代码签名证书。

## 测试质量

1. 增加可调用真实 Tauri 后端的桌面 E2E。当前 Playwright 用例覆盖浏览器壳层；Rust 已覆盖真实 ZIP“压缩 → 解压 → 内容比对”。
2. 前端覆盖率口径已限定为 `src/**/*.{ts,vue}`，当前为 52.32%，并设置 45% 行/语句、60% 分支、38% 函数的防倒退门槛。下一阶段补设置页和解压密码回退分支，逐步达到 70%。
3. 将基于 Mock 的前端性能测试补充为真实 100MB/1GB 文件基准，并记录耗时、峰值内存与回归阈值。

## 功能与工程整理

1. encrypted vault 后端尚无“归档条目”能力；前端已从静默空实现改为明确报错，需要决定是增加持久化字段还是移除该入口。
2. 为确认框和编辑弹窗补统一的焦点陷阱与 Escape 关闭行为。
3. 逐步让主要操作按钮使用统一设计组件。
4. 清理 `src-tauri` 下与压缩应用无关、且不会参与构建的遗留目录与旧报告。

## 本轮已完成

- RAR 加密能力矩阵、运行时行为和 README 已对齐：支持加密 RAR 解压，暂不创建加密 RAR。
- 所有 `#![cfg(any())]` 禁用测试已恢复或由现有有效测试替代。
- 配置服务的初始化、持久化、批量更新、重置、搜索、校验、缓存和导入导出链路已补齐。
- 新增真实 ZIP“压缩 → 解压 → 内容比对”测试，以及缺失输入不得创建空归档的回归测试。
- 密码库 Store 已补齐筛选、排序、收藏、批量删除和使用计数，并修复排序会原地改变源列表的问题。
- 新增压缩页与解压页组件链路测试，覆盖批处理容错、智能子目录和命令参数传递。
- 重做响应式侧栏、工作区标题、投放空状态和 Toast；旧 `appStore` 消息现已桥接到统一可视反馈。
- 修复保险箱解锁按钮无弹窗的阻断问题，并新增主密码表单与回归测试。
- 设置页开关已改为键盘和读屏可识别的语义化 switch，密码格式说明不再误报 RAR 加密压缩。
- Rust 全目标 Clippy 使用 `-D warnings`，CI 同步采用相同标准。
