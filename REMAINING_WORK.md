# 开发收口清单

> 最后更新：2026-07-24 | 当前发布版本：v1.0.12

## 当前结论

核心压缩、解压、密码识别、分卷、取消、事务式输出和更新签名流程已有自动化回归，当前没有已知的发布阻断级故障。公开安装包仍未携带 Windows 11 最外层右键菜单身份包，这是目前唯一尚未对齐的主要产品目标。

## 合入前质量门

1. Draft PR 的 Windows 前端、Rust、Shell Extension、Chromium E2E 和 NSIS 安装包任务全部通过。
2. 在已安装的 Windows 应用中执行人工冒烟：ZIP/7Z 压缩、加密 ZIP/7Z、RAR 解压、密码保险箱、托盘关闭行为、传统右键菜单、卸载和应用内更新。
3. 使用管理员 PowerShell 执行 `npm run test:context-menu-package`，验证测试身份包安装、COM 激活、资源管理器菜单和清理回滚。
4. 合入 CI 后为 `master` 配置分支保护，至少要求 CI 成功且禁止直接覆盖历史。

## 尚未对齐的外部依赖

1. 获取可信的 Windows 代码签名证书，并通过 GitHub Actions Secrets 配置证书、密码和时间戳服务。
2. 有可信证书后启用并发布 Windows 11 新式右键菜单身份包；当前公开版本只保证“显示更多选项”中的传统菜单。
3. 代码签名同时用于降低安装包的 SmartScreen 警告。此项无法仅靠仓库代码完全解决。

## 后续工程工作

1. 增加能够启动真实 Tauri 后端的桌面 E2E；当前 Playwright 验证的是浏览器壳层，Rust 测试验证真实归档引擎。
2. 前端覆盖率当前为 63.77% 行、72.87% 分支、45.75% 函数；优先补 `App.vue`、文件完整性页和 Tauri 命令失败分支，逐步达到 70%。
3. 增加真实 100MB/1GB 文件基准，记录耗时、峰值内存和允许回归阈值；现有前端性能测试主要使用 Mock 和小型夹具。
4. encrypted vault 前端仍将“归档条目”明确报为不支持，而旧密码服务具有归档字段；需要统一数据模型，或者从前端 Store 移除未使用入口。
5. 通用 `Modal` 已覆盖初始焦点、Tab 焦点陷阱、Escape 关闭与焦点归还；后续审计仍未使用通用组件的自定义确认框和编辑弹窗。
6. 分批清理 Rust 历史格式化欠账；当前全仓 `cargo fmt --check` 会产生大量与功能无关的差异，不应混入可靠性修复。
7. Tauri 2、Vite/Vitest 新主版本和 Pinia 新主版本应在独立迁移分支评估，不与当前稳定性收口混合。

## v1.0.12 后已完成

- 密码保险箱密钥目录创建失败改为返回可诊断错误，不再直接 panic。
- 分卷归档文件名无法转换时返回错误，不再通过 `unwrap` 终止进程。
- 新增持续集成质量门：类型检查、覆盖率、生产构建、Rust 全目标测试、Clippy、Shell Extension、Chromium E2E 和无签名 NSIS 构建。
- 删除 4 个长期通过 `cfg(any())` 隐藏、且依赖已移除 API 的腐化测试文件；对应能力由当前数据库/配置集成测试、密码流程测试和服务单测覆盖。
- 本机无 updater、无代码签名证书的 NSIS 构建成功，确认普通 CI 不依赖更新私钥。
- 更新弹窗和全局任务进度条新增关键交互回归；更新安装会在活动任务结束前保持禁用，安装中弹窗不可被关闭。
- 通用模态框修复“首次即为可见状态时不自动聚焦”的问题，并新增 Tab 循环和 Escape 回归测试。

## 已稳定覆盖的核心流程

- 动态归档引擎能力检测和真实 ZIP/7Z/TAR/WIM/扩展格式样本矩阵。
- 未加密归档不会进入密码字典攻击，也不会报告虚假的“密码破解成功”。
- 加密 ZIP、7Z、RAR 解压和密码候选验证；ZIP/7Z/RAR 与 AES 封装的加密创建能力按运行时能力开放。
- 事务式解压暂存、冲突策略、失败回滚、路径穿越和资源上限防护。
- 标准分卷 ZIP、分卷识别、缺失输入保护和取消清理。
- 托盘运行、关闭最小化、一键解压/打包任务分发和传统资源管理器右键菜单。
- 签名应用内更新清单、更新包下载地址和 Release 资产生成。
