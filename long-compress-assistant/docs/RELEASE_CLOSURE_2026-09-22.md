# 1.3.3 发布收尾：历史重试与跨页面图片批次

## 本轮实际完成

1. 从损坏 ZIP 的历史详情粘贴完整包来源、选择新输出目录、创建草稿；确认不自动执行、不修改旧历史；页面启动后内容哈希一致，源包保留，新旧记录在重启后均保留。
2. 100 张 JPEG/PNG/WebP 实际压缩期间，启动真实 7z 归档任务，从压缩中心取消并清除归档。图片任务未取消/删除，100 个输出、100 条历史、34/33/33 格式数量和全部源文件哈希符合预期。
3. 上述跨页面操作复现图片批次进度丢失：任务继续，但页面实例销毁使进度和取消控制器脱离原批次。改为按 task store 隔离的 WeakMap 会话，页面重建共享原进度/控制器，阻止重复启动；不同 Pinia 实例互不污染。

## 验证证据与诚实边界

- 正式单测 58 文件 **379/379**，类型检查/构建通过。
- `test:e2e:desktop:history`：真实引擎、SQLite、页面操作及多次重启通过。原生选择器仅返回路径由已有测试队列提供，未声明原生窗口已验收。
- 历史场景原先未固定“回收源包”开关，完整包被回收后无法用作新来源；改为用户可见操作明确关闭，并断言关闭。不改变生产默认值，不跳过来源检查。粘贴采用 Ctrl+A 替换并核对输入全文。
- 旧图片脚本打开批量设置后未关闭，原生 WebDriver 正确拒绝点击遮挡的开始按钮；补上“保存设置”操作和对话框关闭等待，未用 JS 点击绕过遮挡。
- 图片进度修复后实际 UI 使用“已完成 100/100”和独立 `strong` 百分比字段，旧脚本假定存在分隔符 `·`。按实际结构分别严格断言 100/100 与 100.00%，不改产品文案迎合测试。
- 图片实际并发回归最终通过：100/100 输出与历史，源哈希变化 0，归档取消/清除成功，观察到同时运行的图片任务；完成后的跨页进度仍显示。
- 本机证据：`test-results/desktop-e2e/download-failure-user-result.json`、`image-batch-100-result.json`、`image-batch-100-completed.png`。成功夹具由原脚本清理，代码可重新生成复验；此前失败夹具保留。
- 图片取消控制器跨实例由单测验证，真实混合并发以图片+归档为范围；未把该结果扩称视频/PDF 编码全部重验。

## 发布与接续

- 前一修复提交 `70539d6` 的 Windows CI 35741056464 已通过（7m32s）。本轮提交另等 CI。
- CI 增加单测门禁的 `02f3791` 仍只在本地原分支：GitHub 凭据只有 repo，缺 workflow。现有发布流程无需修改；本轮执行并记录本地完整单测，此自动化欠项单列，不声称已在云端生效。
- 本机旧安装注册表曾标 **1.1.17**，进一步核对实际 EXE/DLL 为 **1.3.1**。用户明确授权后，已保留完整备份并覆盖升级至 1.3.3；程序、唯一 DLL、安装路径和应用数据指纹核对通过，没有按错误注册表版本降级恢复。
- 候选 CI、升级证据与正式发布状态集中见 [1.3.3 发布审计](RELEASE_AUDIT_1.3.3.md)。测试桥 EXE 不作为发行包。

## 发布后优先级（未实现）

1. 失败后对应操作：损坏重新下载、缺卷补卷、目标占用改目录；保留未知原因，不引导所有失败去试密码。
2. 历史可检索范围：先核对后端保存上限与前端 500 条限制，再设计保留策略和分页；不能只加“加载更多”却无法找回已清除数据。
3. 重启后待保存结果恢复：持久待保存队列、去重和明确提示，不自动重跑压缩。
4. 按本轮图片经验检查视频/PDF 跨路由的运行态与取消控制；以实际复现决定修复范围。
5. 版本化保存非敏感任务配置，让历史重建明确哪些参数可恢复，不保存明文密码。
