# D-01.2.2 qpdf 安装态与签名 updater 增量审计

日期：2026-08-30

状态：**完成，D-01 关闭；PDF 转换和 UI 仍未实现，下一节点为 D-02。**

## 原始需求对齐

D-01 要求 qpdf 版本、哈希、许可、安装态能力与安全文档边界可证明。D-01.1 完成官方参数和十类 PDF 基线，D-01.2.1 完成正式资源和生产预检；本步补齐同提交安装包/updater 精确增量，以及正式安装目录的完整、缺失、替换和恢复证据。不提前实现 D-02 UI 或 D-03 转换事务。

## 同提交签名差分

GitHub Actions 运行 `33318192852` 固定在测量提交 `a27ecc0c9fff7968a5d952b5647e25e82ee5f650`。同一 Windows runner、工具链和仓库 updater minisign 密钥先构建只移除 10 个 PDF 资源的基线，再恢复规范配置构建集成包。临时把历史双构建工作流的两个 npm 入口映射到 PDF 脚本，运行启动后立即回滚；产品代码和最终分支不保留该映射。工作流名称仍含历史 C-01 文案，但报告 scope、脚本和移除资源均明确为 D-01 PDF。

| 产物 | 无 qpdf 基线 | 含 qpdf 集成 | 精确差值 |
| --- | ---: | ---: | ---: |
| NSIS | 15,592,042 B；`c46bd10b…c635d` | 19,195,054 B；`6abbd506…b09b` | **3,603,012 B（23.1080%）** |
| updater ZIP | 15,592,202 B；`b0cccab0…02dd` | 19,195,214 B；`2bb5f0dd…7854` | **3,603,012 B（23.1078%）** |

两份 updater 均通过 7-Zip 完整性测试，包内唯一 NSIS 与对应独立 NSIS 字节一致；两份 428 字符签名均为 Tauri minisign 结构。NSIS 不声明 Authenticode 签名。

## 正式安装生命周期

CI `33267974849` 的集成候选为 19,201,569 B / `b679c8d0…1492`。本机从正式安装路径完成：

- qpdf 12.4.0、Apache-2.0、OpenSSL/native、JSON v2、图片优化能力和 10 文件身份全部通过；
- 隔离安装副本分别删除、篡改 qpdf.exe，稳定返回 missing/hash mismatch；正式安装目录只读；
- 候选卸载后恢复公开 v1.1.16、`E:\long\Long解压`、经典菜单、自启动和两处用户数据指纹；最终 `49/49`，失败 0；
- 候选 EXE 隔离副本为 28,914,176 B / `4684370f…64e`。结构化证据位于忽略目录 `test-results/installed-release-validation/20260830-225829/result.json`。

第一次运行的 PDF 矩阵本身通过，但候选卸载后的首次用户目录恢复遇到删除—移动窗口外部重建，脚本拒绝继续并自动恢复公开版本；第二次完整运行 49/49 通过。第一次保留的临时恢复备份因主机安全策略拒绝自动删除，仍位于该次报告所列临时目录；没有改写当前用户数据。

## 其他纠偏与验证

- 本机 Tauri 构建再次在 makensis 阶段返回 `os error 2`，配置由 `finally` 恢复；正式差分只采用干净 GitHub runner 结果。
- 新增 `prepare:pdf-bundle-baseline`、`test:pdf-bundle-delta`、`test:installed-pdf-runtime` 和安装脚本 `-RunPdfRuntimeMatrix`，以后可重复取得同类证据。
- Node 语法、PowerShell AST、媒体依赖/PDF 契约门禁和类型检查通过；正式 PR 仍需完整 CI 后才能合并。

## 下一接续点

D-01 已关闭。下一步严格进入 D-02：先读取现有压缩工作区和公共任务模型，再实现 PDF 输入分析/两模式配置/风险提示；签名默认只分析、加密必须正确密码、默认新文件和禁止原始参数等边界不得放宽。
