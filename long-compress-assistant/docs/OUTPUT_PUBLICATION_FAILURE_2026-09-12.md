# 单文件压缩输出提交失败

## 需求与范围

接续原规划的明确最终失败原因和阶段，基线 `master@1628e50`。本轮仅处理单文件压缩的 `finalize_compression_output` 提交边界，版本 1.3.2、schema 8 不变。

- 提交时发现目标已出现：`output-conflict`，显示“输出目标冲突（未覆盖）”，保留目标路径说明。
- 其他提交异常：`publication-failed`，显示“输出提交失败”，保留底层原因；阶段均为 Publishing，不从最后进度事件推断。
- 取消仍返回 Cancelled。已有路由/校验失败不包装成提交失败，后续磁盘已满归一化保留已确认的提交阶段。
- 历史保存、详情、筛选、诊断导出接入类别，前端不会因详情含密码词汇而请求解压密码重试。
- 测试揭示透明 PublishError::Io 包装不能直接在原因链中取得原始 I/O 类型，已显式取出原始 io::Error 再包装，保留类型与详情。
- 不改变目标冲突策略、事务发布机制或清理顺序。解压的冲突等待、分卷写入、特殊压缩、已完成后清理告警均未改动，不能宣称这些路径的错误已全部结构化。

## 验证与审计

- 前端 7 文件 102/102、类型检查、生产构建、严格 Clippy 和 diff 空白检查通过。
- Rust `publication` 4/4、输出提交事务 5/5、历史命令 12/12、磁盘写满 3/3 通过（过滤器间有重复，不相加为独立用例数）。
- 真实临时文件：已有目标保持原字节，暂存被清理；缺失暂存报告提交失败；取消仍保持取消。正常提交事务回归保留输出内容。
- 可控权限/磁盘写满注入验证类别、阶段和 I/O 原因链；不是实机修改 ACL 或填满用户磁盘。首次原因链断言失败，修正后相关测试全部重跑通过。
- SQLite 使用实际错误类型读回两个类别和 Publishing 阶段。旧记录不猜测回写。
- 提交后追加校验失败回归 3/3 通过，确认校验阶段与取消不被误报为提交失败。
- 未修改用户使用记录或运行用户私有归档，未执行桌面验收矩阵。
- [PR #132](https://github.com/Longyuyeee/long_Decompress/pull/132) 已合入 `master@ed36cd4e2469c93757b3b5c3a74a20e9f562a8c2`。
- [Windows CI 34677986373](https://github.com/Longyuyeee/long_Decompress/actions/runs/34677986373) 通过，用时 5 分 52 秒，安装器产物上传成功；随后交付证据补录仅为文档变更。

## 下一步

继续审计解压提交和冲突恢复路径，区分待处理冲突、提交失败与已提交后清理告警；优先给出真实可操作原因，再考虑跨重启恢复和配置快照。不要据本轮完成宣称所有开发已收口。
