# 1.3.4 发布审计

本轮范围为媒体任务跨页面可靠性，不增加格式或自动恢复功能。原因、旧代码失败及脚本纠偏见 [场景审计](MEDIA_ROUTE_SESSION_2026-09-22.md)。

## 回归结果

- 正式前端 59 文件、387/387，通过类型检查、生产构建。
- Rust 库 405 通过、0 失败、12 按原有条件忽略；严格 Clippy 通过。
- 新增实际 Windows 子进程持有文件的取消测试，确保取消返回后句柄已释放，可立即删除暂存文件。
- 真实视频（114842332 字节）与 PDF（大图 PDF）分别完成页面启动、切历史、返回、取消、重新允许重试；源哈希不变、最终输出不存在、视频/PDF 暂存清理通过。
- 两类取消历史分别完整重启后逐字段一致。本机证据 `test-results/desktop-e2e/video-route-cancel-result.json` 与 `pdf-route-cancel-result.json`；夹具可由脚本重建，用户私有数据不进入仓库。
- 原生选择器响应为已有路径队列替代，编码/优化、文件、进程和 SQLite 为真实执行。未重验原生文件选择窗口、默认播放器/阅读器、卸载生命周期或所有媒体混合并发。

## 候选与公开包

版本身份升至 1.3.4，候选由 GitHub Windows CI 构建。本机 NSIS 工具此前无法启动，沿用 CI 产物下载后本机覆盖升级验证，不用带测试桥 EXE 发布。

公开资产与最终安装记录将在 [v1.3.4 Release](https://github.com/Longyuyeee/long_Decompress/releases/tag/v1.3.4) 维护。候选和公开包是独立构建，不混用哈希。发布完成前不将本页视为公开资产已齐全的证明。

## 后续

见 [下一轮开发顺序](NEXT_VERSION_AFTER_1.3.4.md)。保留 Windows N 暂不支持、CI 单测 workflow 权限欠项，不隐去已知边界。
