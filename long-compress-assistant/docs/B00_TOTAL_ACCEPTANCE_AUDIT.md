# B-00 媒体压缩前置门禁总审计

日期：2026-08-27

公开基线：`1.1.14`

## 1. 总结论

B-00.1 至 B-00.6 全部完成，前置门禁可以收口。当前代码仍保持归档产品能力不变，没有媒体编码引擎、媒体 Tab、第二套队列/历史/事务、占位成功状态或模拟进度进入产品。

B-00 是后续大节点的安全和真实性基础，不是用户可见功能节点，因此本次不提升版本、不构建新 Release。下一步严格进入 B-01 图片依赖与基线实验。

## 2. 六项验收

| 工作项 | 结果 | 关键证据 |
| --- | --- | --- |
| B-00.1 任务/历史兼容 | 通过 | 旧 schema 只增可空字段，旧记录不重写；版本化实测指标与脱敏边界通过 |
| B-00.2 共享事务 | 通过 | 归档压缩先行使用公共同目录暂存/校验/原子发布；系统回收站与失败清理通过 |
| B-00.3 供应链 | 通过 | 四个候选固定来源、版本、哈希、许可和阻断状态；FFmpeg/qpdf 真实身份核对 |
| B-00.4 真实夹具 | 通过 | 4 图片、2 视频、6 PDF 属性差异 0；测试 GPL 工具禁止产品集成 |
| B-00.5 指标来源 | 通过 | 图片/视频/PDF 的进度、大小、速度、ETA 和最终事实来源可执行；估算不入历史 |
| B-00.6 安装与发布 | 通过 | B/C/D 门禁与证据模板可执行；当前 NSIS 生命周期 44/44 并恢复原安装 |

## 3. 总回归结果

- 前端单元测试：41 文件、238 项全部通过；
- TypeScript 类型检查通过；
- Rust 任务历史边界 6/6、共享发布事务 5/5；
- 媒体依赖真实门禁、12 个真实媒体文件事实、Release 身份和媒体架构门禁通过；
- 媒体架构门禁确认产品媒体文件为 0；
- Windows 正式安装生命周期 44/44，失败项 0，恢复版本与路径正确。

## 4. 需求对齐

- 对齐“真实测试”：所有结论有真实文件、真实解析器或真实 NSIS 生命周期证据；未执行的未来媒体能力明确为未实现。
- 对齐“预期与实际差异”：Poppler 发现失败、格式许可偏差和发布证据缺口均保留首次实际与修正记录。
- 对齐“融合现有能力”：未来媒体任务被门禁要求复用任务、历史、发布事务、系统回收站、安装和更新闭环。
- 未发生功能偏移：没有为了看起来完整而加入空 Tab、模拟百分比、未经许可引擎或提前升版。

## 5. B-01 进入条件

B-01 只允许：实际构建已审计 feature 的 libcaesium JPEG/WebP 与 oxipng 无损 PNG 候选；冻结非空输入 SHA-256；测量安装增量、冷启动、内存、质量与真实输出；验证 GIF 明确边界。B-01 未通过前不进入 B-02 UI。
