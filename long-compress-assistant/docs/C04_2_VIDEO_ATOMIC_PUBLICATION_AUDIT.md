# C-04.2 视频原子发布审计

日期：2026-08-28

范围：发布前复检、Mark-of-the-Web、目标竞态、原子发布与最终事实

结论：**C-04.2 已完成；验证后的暂存文件可通过共享事务安全发布，但产品命令/UI 仍未开放。**

## 1. 与共享安全边界对齐

| 要求 | 实际实现 | 状态 |
| --- | --- | --- |
| 只能发布已验证暂存 | API 消费 `StagedVideoOutput + VerifiedVideoOutput`；发布前再次核对源大小、暂存大小和验证字节 | 通过 |
| 发布前取消 | Mark-of-the-Web 前、传播中和原子重命名前均检查同一取消令牌；取消后对象 drop 清理暂存家族 | 通过 |
| 传播下载安全来源 | 复用 `mark_of_web::read_from/propagate_to_tree`；Zone 3/4 ADS 随同一暂存文件原子重命名 | 通过 |
| 不覆盖竞态目标 | 复用 `publish_verified_file`；目标在编码期间出现时返回稳定分类并保留目标原字节 | 通过 |
| 最终事实来自发布文件 | 原子重命名后重新读取最终路径普通文件身份和字节数；输出大小与验证事实必须一致 | 通过 |
| 不提前回收源 | 发布服务不调用回收站；源回收只能由后续完整命令在发布成功且用户明确开启时执行 | 通过 |

发布结果包含最终路径、处理前输入字节、发布后输出字节、真实节省比例、Mark-of-the-Web 状态和 C-04.1 验证事实。估算大小不会进入结果或历史。

## 2. 真实文件矩阵

- 验证通过的真实 VFR/旋转/AAC 输入：同目录原子发布，最终磁盘字节与验证字节一致，源文件保留。
- 目标竞态：验证后现场写入 `existing-user-bytes`；发布拒绝，旧字节逐字节不变，暂存清理。
- 发布前取消：最终路径不存在，暂存清理。
- 真实 NTFS `Zone.Identifier`：源文件 ZoneId=3 和 HostUrl 原字节传播到发布结果，状态为 `applied`。

## 3. 当前边界与下一步

- `cargo test video_publish --lib`：4/4 通过。
- `cargo clippy --lib -- -D warnings`：通过。
- `cargo test --lib`：351 通过、0 失败、4 个显式忽略的外部/破坏性门禁。
- `npm run test:media-architecture`、`npm run type-check`：通过。
- C-04.3 只补齐跨层失败矩阵：编码器非零退出/进程被终止、源或暂存被改写、无效/零字节、发布竞态与容量拒绝均不得发布。通过后关闭 C-04，再进入 C-03.3，将完整编码—验证—发布管线接入统一任务、事件和 UI。
