# 回滚不完整的人工核对指引

## 需求与范围

基线 `master@430e6ac`。上轮已保留回滚不完整的恢复数据并阻止原暂存重试，本轮补齐用户看见错误后的处理说明，不扩展为自动恢复。

- 历史详情仅在已确认 rollback-incomplete 时显示四步指引：保留源/输出/恢复目录、导出诊断、核对新旧文件、必要时选择新的空目录重新解压。
- 指引明确恢复目录可能隐藏，旧记录缺失路径时不要猜测，软件不能自动判断每个备份应该恢复到哪里。
- 保留现有历史解压草稿入口，说明它创建新任务，不恢复旧输出也不提交旧暂存；本轮没有新增强制门禁或改写用户配置。
- JSON 诊断报告新增可空 `recoveryGuidance`，与界面使用同一内容；其他任务为 null。原始错误及来源/输出字段不变，恢复路径仍按记录原文呈现，不解析后自动打开或修改文件。
- 版本 1.3.2、数据库 schema 8 不变；没有扫描用户目录、自动复制/删除文件或回填旧历史。

## 审计与验证

- 定向历史/指引/草稿 35/35；扩大至任务、解压页面与命令层共 6 文件 85/85 通过。
- 生产构建（含 vue-tsc）和 diff 空白检查通过；最终文案调整后的扩大回归再次通过。
- 测试覆盖结构化与标记旧记录、未知错误不猜测、普通任务不出现该指引、只读展示、报告包含步骤与原始恢复路径。
- 未运行用户私有归档、Rust 引擎全量回归或手工桌面矩阵；本轮没有 Rust 产品代码变更。
- 最终代码提交 `d166b74` 的 [Windows package CI 34679686559](https://github.com/Longyuyeee/long_Decompress/actions/runs/34679686559) 通过（含版本核对、NSIS 安装器构建与产物上传）。[PR #136](https://github.com/Longyuyeee/long_Decompress/pull/136) 已合入 `master@777c002`。此后仅补录交接证据，不代表已完成手工安装验收或正式 Release 发布。

## 接续

人工核对说明已提供，但没有自动恢复或逐文件恢复映射。后续应先收敛近期补强范围、做一次主流程综合审计，再决定补丁发布；不要为凑发布版本盲目扩展恢复引擎。普通解压提交错误细分类、跨重启未保存历史恢复和配置快照仍在后续规划中。
