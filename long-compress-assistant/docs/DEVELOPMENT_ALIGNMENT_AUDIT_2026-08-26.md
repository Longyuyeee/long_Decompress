# 开发目标对齐审计（2026-08-26）

## 1. 审计结论

当前开发分支为 `codex/archive-media-roadmap`，公开版本仍为 `v1.1.14`。归档主流程仍然是产品主线，图片、视频、PDF 压缩尚未进入生产代码，因此没有发生以新增功能取代基础压缩解压能力的战略偏移。

S-00.1 至 S-00.4 及跨步骤总验收已经完成：保险箱本机保护、归档密码领域模型、任务/历史边界、格式支持等级和旧工程归档均有真实 Windows 证据，正式 NSIS 覆盖与恢复 48/48 通过。下一步进入媒体压缩 B-00 架构门禁，但仍不能直接实现媒体引擎。

## 2. 最初基本需求对齐表

| 基本需求 | 当前代码与真实证据 | 结论 |
| --- | --- | --- |
| 压缩、解压主流程可靠 | 正式安装态覆盖 ZIP、加密 7Z/RAR、TAR/TAR.GZ、中文八层路径、混合内容、危险/损坏场景和 18 万条目取消 | 已对齐 |
| 不把密码尝试伪装成解压进度 | 密码候选由后端状态机统一处理；真实输出前不推进解压百分比 | 已对齐 |
| 分卷、未加密归档不误入字典流程 | 通用引擎使用结构化目录元数据识别加密；探测超时明确失败 | 已对齐 |
| 密码保险箱自动参与解压 | 后端读取文件型密码本，优先收藏、高频和最近使用项；命中后更新调用次数、最近使用时间和本地自然日趋势；正文使用 DPAPI 保护的安装密钥与 AES-256-GCM 保存 | 已对齐 |
| 压缩/解压详情保持左右结构、只纵向滚动 | 已有窄窗口、150% 缩放和无横向溢出的真实桌面门禁 | 已对齐 |
| 浏览压缩包内部内容 | 已有目录树、搜索、类型筛选、右键动作、默认应用打开、受限内部预览、三层嵌套和精确选择性解压 | 已对齐 |
| 历史任务与版本识别 | 历史路由、侧栏入口、详情、筛选及左下版本角标均已落地 | 已对齐 |
| 解压完成后删除源文件但允许恢复 | 仅在完整成功且不属于部分/容错/跳过冲突模式时，将全部源分卷移入 Windows 系统回收站 | 已对齐 |
| Explorer 高频操作 | 经典菜单保留 17 条子命令、4 条快捷命令和“浏览压缩包内容”入口 | 已对齐 |
| Windows 11 第一层菜单 | 无签名身份时无法可靠公开部署；无证书构建不携带失效 MSIX 身份包 | 外部身份限制，边界诚实 |
| 安装、更新和回滚真实可用 | 已完成真实 `1.1.13 → 1.1.14 → 卸载 → 1.1.13`，公开更新 24 项检查通过 | 已对齐 |

## 3. 已发现的偏移与风险

### P1：密码保险箱保护语义与实际存储不一致（S-00.1 已解决）

`README.md` 仍声明移除主密码后“继续使用安装实例密钥保护本机数据”，但 `EncryptedPasswordService` 的新增、更新和文件保存路径会把 `PasswordEntryDb` 直接序列化到 `passwords/*.json`；Windows 权限收紧函数当前也是空实现。

用户取消手工锁定和主密码输入的决定应继续保留，但“无感使用”不能被描述成“已经加密保护”。S-00 必须二选一并完成迁移验证：

1. 优先方案：使用 Windows 当前用户级保护（例如 DPAPI）保护密码正文，启动时无须用户输入主密码；
2. 降级方案：明确更名为本地解压密码本，删除安装实例密钥保护的公开声明和未生效的加密外观。

S-00.1 已采用优先方案落地：Windows 当前用户 DPAPI 保护随机安装密钥，密码正文使用 AES-256-GCM，WebView 不再接触安装密钥；旧数据迁移、篡改拒绝、真实应用重启和加密 7Z 密码命中均已通过。证据见 [PASSWORD_VAULT_PROTECTION_S00_1_AUDIT.md](PASSWORD_VAULT_PROTECTION_S00_1_AUDIT.md)。

### P1：B-00 只有方向，没有可执行门禁

路线图已经提出 `workloadKind`、版本化指标、第三方引擎许可与发布事务复用，但此前没有独立的 B-00 工作项和验收目标。若直接实现图片压缩页面，会把模型迁移、任务取消、历史兼容、依赖来源和安装包体积留到最后处理。

本次已在主路线图补充 B-00。B-00 只允许模型、迁移、依赖清单、事务边界和测试夹具设计，不允许接入图片、视频或 PDF 编码引擎。

### P2：传统密码管理器模型残留（S-00.2 已解决）

此前前端 `src/stores/password.ts` 包含 `username`、`url`、传统网站密码强度、过期与通用健康分析字段。当前用户界面已经切换到归档密码调用、归档线索和使用趋势语义，因此这些字段只允许作为旧磁盘数据兼容债务，不能成为新统计或新 API 的基础。

S-00.2 已定义归档密码活动模型；旧字段只在 Rust 磁盘兼容模型中读取和保留，不再进入新增、编辑、搜索或统计接口。

### P2：文档与仓库卫生（S-00.3/S-00.4 已解决）

- `DEVELOPMENT_HANDOFF.md` 是按时间追加的历史记录，旧段落中的“当前版本”只代表当时状态；接手必须以文件顶部和本审计为准。
- HFSX 已使用固定工具提交生成非空载荷，并通过随包 7-Zip 和 Windows Release Tauri 逐字节验证；当前权威口径见 `FORMAT_SUPPORT_LEVELS.md`，旧结论仅保留为历史记录。
- `src/stores/task.ts` 的无实现、无调用 `fetchTasks` 占位接口已删除，历史任务仍由独立 store 和 SQLite 提供。
- `src-tauri/TranslateSoftware` 经零构建引用检查后已作为 Git 重命名移至 `archive/legacy-projects/TranslateSoftware`，不再混入产品源码目录。

## 4. 下一步顺序

### S-00：基础能力语义收口

1. **已完成 S-00.1**：Windows DPAPI + AES-256-GCM 无感保护、旧明文迁移和失败保留；
2. **已完成 S-00.2**：活动密码模型收敛为归档密码语义，传统密码管理字段仅保留为磁盘兼容数据；
3. **已完成 S-00.3**：删除任务 store 的历史读取占位接口，真实历史数据源保持唯一；
4. **已完成 S-00.4**：统一 README、交接文档和格式支持等级，补齐 HFSX 非空真实载荷，并归档未参与构建的旧工程；
5. **已完成 S-00 总验收**：真实密码命中、当天趋势、升级迁移、应用重启和正式安装覆盖均已复验，完整预期、首次实际、修正和最终实际见 `S00_TOTAL_ACCEPTANCE_AUDIT.md`；
6. S-00 不提升版本，除非产生用户可见修复且完整发布门禁通过。

### B-00：媒体压缩架构门禁

S-00 通过后，按照主路线图新增的 B-00 定义完成任务/历史兼容设计、第三方依赖身份许可审计、统一事务边界和真实样本基线。B-00.1 任务/历史兼容模型已于 2026-08-27 通过真实数据库快照验收，证据见 `B00_TASK_HISTORY_MODEL_AUDIT.md`；下一步是 B-00.2 共享事务边界审计。B-00 总审计通过前不实现媒体编码界面和引擎。

## 5. 审计边界

本节记录 2026-08-26 首次对齐审计当时的边界：当次仅做文档和代码静态审计，没有重新执行耗时的正式安装、公开更新及全格式矩阵。其后 S-00 已按要求完成真实 Windows 总验收，当前权威证据为 `S00_TOTAL_ACCEPTANCE_AUDIT.md`，不应再把本节的历史边界解读为当前未验收。
