# Long解压 v1.1.13 发布审计

审计日期：2026-08-22

## 结论

v1.1.13 的代码、真实样本与关键桌面交互门禁均已通过。正式资产必须在版本身份、安装器内容、updater 签名和公开 `latest.json` 全部复核后才发布。

## 需求对齐

| 范围 | 实际结果 | 判定 |
| --- | --- | --- |
| 解压进度 | 密码候选保持 0，真实写出后按字节推进 | 已完成 |
| RAR 正确密码 | 无固定短超时，真实官方样本正确/错误密码均验证 | 已完成 |
| 暂存目录 | 立即隐藏，清理后刷新 Explorer | 已完成 |
| 冲突处理 | 逐项决策直接提交现有暂存，不重复解压 | 已完成 |
| 容量预检 | Win32 后备覆盖不存在目标子目录 | 已完成 |
| 历史审计 | 保存实际格式并有界保留长候选关键记录 | 已完成 |
| 旧历史 | 不伪造、不反向迁移，继续反映旧版本当时数据 | 已明确 |

## 候选验证

- 前端 37 个测试文件、218 项测试通过；TypeScript 类型检查和正式生产构建通过。
- Rust 全目标 373 项通过、7 项按设计忽略；真实加密 RAR 测试显式启用；Clippy 全目标零警告。
- 真实 Windows Tauri/WebView2 的资源预检、归档闭环、历史持久化、保险箱使用和加密 RAR 五组聚焦门禁通过。
- 当前终端非管理员，未执行会临时信任开发证书的 Windows 11 第一层 MSIX 部署测试；公开构建不包含该未签名身份包。
- 当前开发机不能提供有效 Defender 扫描证据，该项保留为启用 Defender 的干净机发布后验证。

## 正式资产门禁

- npm、Tauri、主程序、Shell Extension、两个 Cargo 锁文件和版本化 DLL 必须统一为 `1.1.13`。
- GitHub Release 必须包含 NSIS 安装包、updater ZIP、签名文件和可解析的 `latest.json`。
- `latest.json` 的版本、下载 URL 与签名必须指向同一公开 Release。
- 安装包大小、SHA-256 和公开资产复验结果在正式构建后补入本审计。
