# Long解压 v1.1.13 发布审计

审计日期：2026-08-22

## 结论

v1.1.13 的代码、真实样本与关键桌面交互门禁均已通过。正式资产必须在版本身份、安装器内容、updater 签名和公开 `latest.json` 全部复核后才发布。

## 需求对齐

| 范围 | 实际结果 | 判定 |
| --- | --- | --- |
| 解压进度 | 密码候选保持 0，真实写出后按字节推进 | 已完成 |
| RAR 正确密码 | 无固定短超时，真实官方样本正确/错误密码均验证 | 已完成 |
| 暂存目录 | 立即隐藏，清理后刷新 Explorer | 已完成 |
| 冲突处理 | 逐项决策直接提交现有暂存，不重复解压 | 已完成 |
| 容量预检 | Win32 后备覆盖不存在目标子目录 | 已完成 |
| 历史审计 | 保存实际格式并有界保留长候选关键记录 | 已完成 |
| 旧历史 | 不伪造、不反向迁移，继续反映旧版本当时数据 | 已明确 |

## 候选验证

- 发布前前端聚焦套件为 37 个文件、218 项通过；发布后普通 CI 暴露新增格式事件对应的监听器数量断言仍停留在 4，测试修正并增加格式落库断言后，完整 coverage 套件 40 个文件、240 项全部通过。TypeScript 类型检查和正式生产构建通过。
- Rust 全目标 373 项通过、7 项按设计忽略；真实加密 RAR 测试显式启用；Clippy 全目标零警告。
- 真实 Windows Tauri/WebView2 的资源预检、归档闭环、历史持久化、保险箱使用和加密 RAR 五组聚焦门禁通过。
- 当前终端非管理员，未执行会临时信任开发证书的 Windows 11 第一层 MSIX 部署测试；公开构建不包含该未签名身份包。
- 当前开发机不能提供有效 Defender 扫描证据，该项保留为启用 Defender 的干净机发布后验证。

## 正式资产门禁

- npm、Tauri、主程序、Shell Extension、两个 Cargo 锁文件和版本化 DLL 已统一为 `1.1.13`；发布身份脚本通过。
- 主程序 FileVersion/ProductVersion 均为 `1.1.13`，ProductName 为 `Long解压`，Authenticode 状态按既定无商业证书策略为 `NotSigned`。
- NSIS 安装包包含 14 个载荷文件，7-Zip 完整性测试通过；大小为 7,714,218 字节，SHA-256 为 `6E9F55DA079D2E592779A9C9FB0DB846E3BD5EA4E31EEB45D18E34D882D9B57B`。
- updater ZIP 大小为 7,714,378 字节，SHA-256 为 `9BC64159977677E50EB68EE7A5A8742BBA3063414B4B19EFC8A3F5E66EB916DC`；独立 `.sig` 已生成，Tauri 配置公钥与仓库忽略的本地公钥备份逐字一致。
- 上述大小与哈希属于本地独立候选。GitHub Actions 在干净 Windows runner 重新构建并成功发布四项资产；公开安装包大小为 7,634,242 字节，SHA-256 为 `F93908955014F88E524C4E5761D96DC747B5D0127248A195702BE38EEA760062`。
- 从公开 Release 重新下载的 NSIS 安装包和 updater ZIP 均通过 7-Zip 完整性测试；`latest.json` 版本为 `1.1.13`，下载 URL 指向同一标签，内嵌签名与公开 `.sig` 逐字一致。
- Release 工作流 `32581439992` 全部通过，发布页为 <https://github.com/Longyuyeee/long_Decompress/releases/tag/v1.1.13>。
- 本机已通过应用内公开更新界面从已安装 `1.1.12` 升级到 `1.1.13`：22 项检查全部通过，覆盖更新交接、安装版本、自动重启、4 个经典菜单根的 17 条子命令与 4 条快捷命令、唯一版本化 Shell DLL、无 MSIX 身份包及两处用户数据逐文件指纹不变。
- 首次公开更新复验还暴露验证脚本依赖 `Get-FileHash` 自动加载的问题；验证器已改为直接使用 .NET SHA-256 文件流并在同机复验通过。该修复只影响开发验证脚本，不改变已发布应用二进制。
