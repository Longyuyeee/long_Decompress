# 归档队列、配置来源与停止语义审计（2026-09-04）

## 本轮目标与真实根因

本轮按用户提出的四项问题审查真实前后端代码，没有把界面现象直接当作原因：

1. **显示顺序与执行顺序不一致**：`AeroTable` 已按中文数字自然顺序显示，但 `DecompressView` 仍按任务创建顺序启动，因此界面第一项不一定先运行。
2. **并发设置看似失效**：`maxConcurrentTasks` 确实传入 `runArchiveTasks`；不同输出资源会按上限并行，但同一输出目录会被安全资源键串行化，默认“同目录”时容易被误认为设置无效。
3. **全局与单独配置来源不清**：压缩中心内部用 `settings === undefined` 表示跟随全局，但界面没有明确切换；解压任务只保存配置快照，没有记录来源。
4. **停止与暂停混淆**：列表发出了单项停止事件，但解压父视图没有接线；全局停止又逐项等待后端清理，后续任务不能及时收到停止信号。后端只有取消令牌，没有统一的可恢复暂停协议。

## 已完成修改

- 新增共享自然排序工具，解压列表显示和启动队列使用同一比较器；名称相同再以任务 ID 稳定排序。
- 排队任务可在前端直接结束，不再错误调用尚不存在的后端进程；已被停止的排队任务取得并发槽后会被跳过。
- 解压单项停止事件已经接到任务仓库；解压与压缩的全局停止均并行发出停止请求，解压全局停止同时结束等待任务。
- 等待状态由暂停符号改为时钟；操作文案明确为“停止全部”和“停止此任务”。
- 解压任务新增显式 `global / individual` 配置来源；压缩组和单文件任务新增同样的显式切换。切回全局会清除单项覆盖，切到单独会复制当前全局值作为可编辑快照。
- 设置中心新增默认压缩格式、默认压缩等级、解压默认同名文件夹；这些字段进入现有本地与后端设置持久化。新压缩草稿从持久化默认值开始。
- 智能解压目录判断继续保留：它根据每个压缩包的真实根条目数决定同名文件夹；显式操作全局同名文件夹开关时仍会覆盖所有跟随全局的待处理任务。

## 并发边界

并发上限不是“无条件同时写入同一个文件夹”。当前事务先写独立 staging，再提交到最终目录；同一最终目录仍可能发生同名冲突、覆盖/改名策略竞争，因此现阶段继续串行提交是正确的安全边界。不同输出目录的任务会按设置并行。

若后续要求同一目录也并行，必须先在后端实现目录级提交仲裁或按最终子目录计算更细资源键，并用同名冲突真实样本验证；不能仅删除前端资源锁。

## 预期 / 实际验证

| 验证 | 预期 | 实际 |
| --- | --- | --- |
| 自然排序 | `1 (1)`、`1 (2)`、`1 (3)` 的显示与执行顺序一致 | 聚焦测试按相同顺序调用真实命令封装 |
| 排队停止 | 不调用不存在的后端进程，任务进入 `cancelled` | 通过 |
| 全局/单独配置 | 切换来源后有效配置明确、可恢复全局继承 | 压缩 store 与解压视图测试通过 |
| 设置默认值 | 格式、等级、同名目录跨启动持久化 | app store 与设置视图测试通过 |
| 相关前端回归 | 无既有行为回归 | 8 个文件、81 项全部通过 |
| 真实图片夹具 | 11 张允许图片和 1 个 PDF 拒绝边界保持一致 | 通过 |
| 类型检查 | Vue / TypeScript 无错误 | 通过 |
| 带测试桥生产前端 + Rust Release | 可启动真实 Tauri/WebView2 | 构建通过，真实应用成功启动 |
| archive-flow 桌面门禁 | 并发、ZIP 遥测、同目标串行、加密往返全部通过 | 真实 Windows Release 应用通过 |

首轮桌面门禁发现测试仍要求 ZIP 最终完成事件携带瞬时速度，但后端为避免历史上的天文数字，已明确只在至少 250 ms 的非终态稳定观察窗发布速度与 ETA。实际中间事件已有真实速度；最终事件正确提供完整字节数并将不稳定速度置空。桌面门禁现已对齐该产品契约：稳定中间事件必须有实测速率，终态不得生成单样本速度尖峰。复跑通过，失败截图和其他机器临时产物继续保持忽略、不提交。

## 暂停 / 恢复补充闭环

- 新增统一任务控制器，取消、暂停和恢复共享同一任务注册表；停止会唤醒暂停等待，避免死锁。
- 原生读写、密码保险箱/词表、ZIP/7Z 密码验证、完整性校验和发布前检查都进入安全暂停点。
- Windows 外部 7-Zip、WinRAR、RAR 和分卷子进程使用系统级挂起/恢复；真实子进程测试证明暂停期间输出字节不增长。
- 任务列表、工具栏与左下任务监控只对归档任务展示暂停/恢复；特殊压缩不会得到只改状态的假暂停。
- 原生 RAR 库受 API 边界限制，只能在条目之间暂停；外部 RAR 引擎可以即时挂起。该边界已写入发布说明，不隐藏为“完全即时”。

## 最终预期 / 实际

| 验证 | 预期 | 实际 |
| --- | --- | --- |
| 原生控制 | 暂停阻塞、恢复继续、停止唤醒 | 2/2 通过 |
| 外部进程 | 暂停窗口内真实写入停止 | Windows 子进程实测通过 |
| 桌面交互 | 行内与全局暂停/恢复/停止都作用于后台 | 真实 Tauri/WebView2 通过 |
| 停止清理 | 取消终态、临时输出清理、队列接续 | 通过 |
| 完整前端回归 | 状态与配置无既有回归 | 53 文件、303/303 通过 |
| Rust 全量 | 无告警、常规测试无失败 | 严格 Clippy 通过；主库 389 通过、10 项专用环境测试明确忽略，集成测试通过 |
| archive-flow | 原有并发与加密归档主流程无回归 | 真实 Windows Release 应用通过 |

## 版本结论

队列顺序、并发边界、配置来源、单项/全局停止以及真正的归档暂停/恢复均已闭环，版本提升为 `v1.2.8` 候选。发布证据和公开状态以 [RELEASE_AUDIT_1.2.8.md](RELEASE_AUDIT_1.2.8.md) 为准。
