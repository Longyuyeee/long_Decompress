# 1.3.6 发布审计

本轮交付历史保留与分页基础，范围拆分和旧代码复现见 [场景审计](HISTORY_RETENTION_2026-09-23.md)。全库搜索/统计仍在下一版计划，不把本轮写成整个历史重构已完成。

## 验证事实

- 前端 60 文件、406 项通过；Rust 库 406 项通过、12 项按既有条件忽略；严格 Clippy 通过。
- 旧后端保存 505 条后只余 500，新增测试先红；移除自动清理后重启完整保留，分页无遗漏/重复。覆盖同时间戳、新记录插入、删除游标及有界页大小。
- UI/store 覆盖错误重试、无匹配继续加载、刷新/清空/删除竞态，以及清空失败保留原列表。
- `node scripts/test-tauri-desktop.mjs --history-only` 通过：真实 ZIP 往返、损坏指引、缺卷阻断、新草稿手动执行、旧失败不变；另用 505 条合成历史验证真实 SQLite 保存→重启→搜索无匹配→加载至第 500 条之外→查看旧错误。
- 本机证据 `test-results/desktop-e2e/history-paging-result.json` 和 `download-failure-user-result.json`；选择器响应来自测试桥，非原生选择器验收，合成记录非实际执行任务。窄窗口溢出检查通过且截图已复核。
- 生产前端构建、Shell 扩展构建及 8 处版本身份检查通过。无数据库 schema 修改，旧记录不批量重写，旧查询接口保留最多 500 条读取兼容语义。

## 发布和边界

版本身份和 README 为 1.3.6，正式包由 GitHub Release 工作流生成。公开附件及安装升级的最终事实维护于 [Release](https://github.com/Longyuyeee/long_Decompress/releases/tag/v1.3.6)，本页不提前证明公开资产已生成。带测试桥的本机程序不用于发布，用户备份不上传。

历史会占用磁盘；旧版已删除记录不能恢复，降级可能再次自动删除。已加载记录仍积累在内存；游标不是事务快照，修改记录后以刷新为准。全库筛选、聚合及可配置保留策略尚未完成。

下一步见 [1.3.6 后计划](NEXT_VERSION_AFTER_1.3.6.md)。单测 CI workflow 权限欠项仍存在，本地 `02f3791` 不算已启用云端单测门禁。
