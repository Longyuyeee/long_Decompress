# Long解压开发审计与后续计划

> 审计日期：2026-07-31  
> 审计基线：`master` / `5f4505686a8ea0770b5de5178d9ad6433967fb4e`  
> 当前正式版本：v1.0.20  
> 当前文档分支：`agent/development-audit-2026-07-31`

## 1. 当前结论

v1.0.20 已经完成正式发布，可以作为当前稳定基线继续向后开发。

- PR #36 已合并到 `master`，`v1.0.20` 标签指向合并提交 `5f4505686a8ea0770b5de5178d9ad6433967fb4e`。
- GitHub Release 已发布：[Long解压 v1.0.20](https://github.com/Longyuyeee/long_Decompress/releases/tag/v1.0.20)。
- Release 工作流已生成并校验 `Long-Decompress_1.0.20_x64-setup.exe`、`.nsis.zip`、`.sig` 和 `latest.json`。
- `latest.json` 已指向 `1.0.20` / `windows-x86_64`，签名 updater 资产在线可用。
- 已从本机公开安装的 v1.0.19 执行 `test:public-update` 升级到 v1.0.20，应用内更新、安装、重启、安装路径保持、用户数据保持和传统右键菜单资源检查均通过。
- 当前 GitHub 无开放 PR、无开放 Issue，本地 `master` 与 `origin/master` 对齐。
- `npm audit --omit=dev` 为 0；完整 `npm audit` 仍有 15 项开发工具链漏洞，修复需要跨主版本升级 Vite/Vitest/vue-tsc 等工具链。

本轮审计没有发现阻塞 v1.0.20 继续使用的 P0 问题。下一阶段应从“发布收口”转入“发布后治理、性能趋势、桌面自动化基础设施和依赖现代化”。

## 2. 已对齐的开发目标

### 核心归档流程

- 解压、压缩、密码解压、密码压缩、取消、磁盘写满、冲突策略、事务提交和失败回滚均已有回归保护。
- ZIP、7Z、TAR 系列、单文件流和项目自有 AES 包装格式已迁入独立原生模块，`compression_service.rs` 已从最初的 4,281 行降到约 2,436 行。
- 7Z 密码/损坏分类、时间戳、仅解压较新文件、过滤、取消、跳过损坏和暂存回滚已补齐真实回归。
- RAR 密码验证已加入限时原生预检，错误密码不会长期卡死，也不会把密码传入外部 CLI 命令行。
- 通用 7-Zip 引擎的加密检测已改为非交互执行并设置超时，避免隐藏控制台或外部进程卡住主流程。

### Windows 集成与交互

- 支持托盘运行、关闭最小化、第二实例恢复、活动任务退出确认、活动任务期间阻止更新安装。
- 传统资源管理器右键菜单和一键解压/压缩链路已进入安装版验收；Windows 11 顶层右键菜单仍受可信代码签名证书限制，当前公开安装包继续只交付传统菜单。
- 更新中心已经具备版本检测、签名校验、稍后提醒、跳过版本、任务保护和应用内更新入口。
- 配置组、密码保险箱、文件完整性、全局任务进度和压缩中心任务生命周期均已有关键交互测试。

### 发布治理

- `master` 通过 PR 合入，Release 由 `v*` 标签触发。
- Release 工作流会校验版本身份、运行前端和 Rust 检查、构建 NSIS/updater 资产、校验 Windows 11 身份包跳过策略并发布 `latest.json`。
- v1.0.20 发布后已完成真实公网应用内更新验收，证据目录位于 `long-compress-assistant/test-results/public-update-validation/20260730-234442`。

## 3. 当前风险与缺口

| 优先级 | 风险/缺口 | 当前状态 | 下一步 |
| --- | --- | --- | --- |
| P0 | 发布后可追溯记录仍分散 | v1.0.20 已有工作流、Release 和本机更新验收，但 Release 页面尚未强制回链验收证据 | 每个 Release 固定创建/回链验收记录，最好由模板或脚本生成摘要 |
| P1 | 真实桌面 E2E 仍依赖交互式 Windows 机器 | GitHub 托管 runner 可构建桌面 E2E 二进制，但无法稳定创建 WebView2 调试端口 | 准备固定交互式 Windows self-hosted runner，接入严格桌面矩阵 |
| P1 | 性能基线尚未形成趋势 | 已有 100 MiB、1 GiB 和小文件基准，但没有固定机器连续样本 | 在同一 Windows 环境积累至少 10 次样本，再设置告警阈值 |
| P1 | HFSX 仍缺少可再分发、非空、可逐字节核验的可靠样本 | 严格矩阵已覆盖可生成场景；HFSX 不能只凭空镜像或文件头宣称完整支持 | 寻找可信生成工具或可再分发样本，补齐后再公开声明完整通过 |
| P2 | 开发工具链存在 15 项 npm audit 漏洞 | 生产依赖为 0；漏洞集中在 Vite/Vitest/vue-tsc/@vue/test-utils 等开发链路 | 独立迁移分支处理，禁止与归档语义修复混合 |
| P2 | Tauri 2 / Windows 11 顶层菜单 / 商业签名仍未启动 | 当前功能不被阻塞，但用户体验和 SmartScreen 仍受影响 | 先评估证书成本和签名流程，再启用 Windows 11 身份包 |
| P2 | `ParallelExtractor` 仍是实验实现 | 没有完全对齐密码、冲突、时间戳、路径安全、资源限制和事务回滚语义 | 要么补齐生产语义后接入，要么删除实验入口，避免长期双实现 |

## 4. 下一阶段推荐路线

### 阶段 R：v1.0.20 发布后治理

目标：让发布结果、自动化、人工验收和用户可见文档互相可追溯。

1. 为 v1.0.20 补一份正式验收记录，回链 Release、Actions run、资产清单、`latest.json` 校验和 `test:public-update` 证据。
2. 将 `RELEASE_CHECKLIST.md` 从“手填模板”推进到“可生成结果摘要”的半自动流程。
3. 每次 Release 后固定保留上一正式版本环境，执行应用内更新验收再关闭发布任务。
4. README 只保留用户可见状态，详细工程证据放入 `docs/` 或根审计文档。

验收：任何一个正式 Release 都能从 GitHub Release 反查到 CI、安装包、updater、应用内更新和人工验收结论。

### 阶段 P：性能趋势与资源回归

目标：把“感觉更快/更稳”变成可比较的数据。

1. 固定 Windows 机器、固定电源策略、固定样本目录和固定工具版本。
2. 周期运行大文件、小文件、AES、7Z、ZIP 和真实解压矩阵的性能采样。
3. 先积累至少 10 次样本，再建立趋势告警；不要用共享 runner 单次波动阻断发布。
4. 为压缩、解压、密码处理、外部引擎调用分别记录吞吐、峰值工作集、失败分类和清理结果。

验收：能比较 v1.0.20 之后每个版本的核心吞吐、内存峰值和失败恢复趋势。

### 阶段 W：桌面 E2E 基础设施

目标：把目前依赖本机手动触发的真实桌面矩阵迁入稳定值守环境。

1. 准备 self-hosted Windows runner，安装 WebView2、EdgeDriver、7-Zip 相关测试工具和必要 WSL/QEMU 工具。
2. 将 `test:e2e:desktop:full-format` 划分为 PR 快速门禁、夜间严格矩阵和 Release 前完整矩阵。
3. 保存失败截图、执行日志、输出目录指纹和关键样本哈希，减少失败后人工复盘成本。
4. 保留“缺少任一生成器则严格模式失败”的原则，避免空壳样本误判为支持。

验收：严格桌面矩阵可在固定 Windows 环境无人值守运行，失败时能定位到格式、任务阶段和输出证据。

### 阶段 M：依赖与平台现代化

目标：在不扰动归档语义的前提下升级开发/桌面平台。

1. 先开独立分支评估 Vite/Vitest/vue-tsc/@vue/test-utils 主版本升级。
2. 单独评估 Tauri 2 迁移，不与依赖审计修复混在同一个 PR。
3. 每个迁移 PR 都必须重新跑安装器、updater、托盘、右键菜单、解压/压缩/密码矩阵。
4. 获得可信代码签名证书后，再启用 Windows 11 顶层右键菜单身份包，并重新验证 SmartScreen、升级和卸载。

验收：工具链漏洞减少，同时用户可见功能和发布资产不回退。

### 阶段 F：格式样本继续扩展

目标：只声明经过真实载荷闭环验证的格式支持。

1. 优先补 HFSX 非空样本或可信写入工具。
2. 对曾经因“只能识别文件头/空镜像/损坏样本”而移除公开声明的格式，逐个补真实样本后再恢复文案。
3. 样本必须可再分发、固定 SHA-256、包含已知文件，并在桌面矩阵中逐字节核验。

验收：格式支持表与真实矩阵一一对应，不再出现“界面声称支持但主流程无法闭环”的情况。

## 5. 当前不建议做的事

- 不在 v1.0.20 刚发布后立即混入大规模功能重构。
- 不把 Tauri 2、Vite/Vitest 主版本迁移和归档引擎语义修复放进同一个 PR。
- 不在没有可信证书时强行分发 Windows 11 顶层右键身份包。
- 不用空镜像、文件头识别或损坏样本证明格式完整支持。
- 不让实验性的 `ParallelExtractor` 在未对齐生产语义前进入主流程。

## 6. 建议的下一步

1. 先合并本轮文档审计，修正文档中 v1.0.20 已发布后的状态漂移。
2. 下一轮进入阶段 R：补齐 v1.0.20 Release 验收记录回链和发布证据摘要。
3. 随后进入阶段 P/W：建立固定 Windows 性能趋势和 self-hosted 桌面 E2E。
4. 最后再单独开阶段 M：依赖主版本迁移和 Tauri 2 评估。
